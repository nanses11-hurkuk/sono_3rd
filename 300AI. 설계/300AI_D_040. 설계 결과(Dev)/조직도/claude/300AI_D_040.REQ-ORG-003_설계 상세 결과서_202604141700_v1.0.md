# [설계 상세 결과서] REQ-ORG-003

> **산출 기준**: claude(v1.0) + gemini(v1.1) 설계 수행보고서 통합 최종본

---

## (1) 기본 정보

| 항목 | 내용 |
|---|---|
| 요구사항 ID | REQ-ORG-003 |
| 모듈(개발)별 설계 요청 ID | 300AI_D_010.REQ-ORG-003_설계 요청서_202604091600_v1.0 |
| 모듈(개발)별 설계 계획 ID | DP-ORG-003 |
| 모듈(개발)별 설계 보고 ID | DR-ORG-003 |
| 모듈(개발)별 설계 상세 결과 ID | DRS-ORG-003 |
| 모듈(개발)별 코드 | ORG |
| 개발팀 | 커뮤니케이션팀 (솔루션 개발본부) |
| 개발자(담당자) | 이윤정 |
| 작업일 | 2026-04-14 |
| 최종 승인자 | 상세 설계 단계에서 협의 및 정의 필요 |
| 최종 승인일 | 상세 설계 단계에서 협의 및 정의 필요 |

---

## (2) 개발 최종 설계 내용

### 1. 아키텍처 (컴포넌트 구조 · 시퀀스 다이어그램) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

#### 컴포넌트 구조

```
[포탈 조직도 UI]   [전자결재 결재선 선택]        [이음톡 대화 상대 검색]
        │           (CompanyCode=ALL 조건)       (MobileOrgCon 활용)
        │                   │                        │
        └───────────────────┴────────────────────────┘
                            │
                            ▼ 기존 API 재활용 (파라미터 조건 변경)
           [UnifiedOrgTree] ─ [OrgManageCon / OrgApiCon / MobileOrgCon]
                               (IF-COV-USR-238~941 기존 컨트롤러)
                            │
                            ▼
                    [OrgSvc - 도메인 통합 쿼리 지원]
                    [coviTree - 통합 조직도 UI 컴포넌트 (Lazy Loading 적용)]
                            │
                            ▼ (sys_base_config.IsGlobalOrgChart=Y 시 도메인 필터 우회)
                    [covi_syncdata]
                   orgs_orgperson (UserCode, CompanyCode PK)
                   orgs_orgdept   (GroupCode, CompanyCode PK)
                   → V1·V2 데이터 동일 테이블 공존

                  [공통 서비스]
                  F-COR-SHR-001: 조직도 컨트롤 팝업 (IF-COV-USR-243)
                  F-COR-SHR-002: 사용자 사진 조회 (IF-COV-USR-385)

                  [ScopeFilter: 메뉴별 노출 범위 보안 필터]
```

#### 시퀀스 다이어그램 (전자결재 결재선 타 계열사 임직원 선택)

```
전자결재 UI        공통 조직도 팝업           OrgManageCon         covi_syncdata
    │              (IF-COV-USR-243)              │                      │
    │─ 결재선 추가 클릭 ──▶                      │                      │
    │              │─ getInitOrgTreeList.do ─────▶                      │
    │              │   CompanyCode=ALL 조건       │─ SELECT orgs_orgdept │
    │              │                              │   WHERE 1=1(전체)   ─▶
    │              │                              │◀─ V1+V2 부서 목록 ───│
    │              │◀─ 통합 조직도 트리(V1+V2) ───│                      │
    │              │                              │                      │
    │              │ [V1 임직원 선택]              │                      │
    │              │─ getUserList.do ─────────────▶                      │
    │              │   CompanyCode=V1             │─ SELECT orgs_orgperson│
    │              │                              │   WHERE CompanyCode='V1'▶
    │              │                              │◀─ V1 사용자 목록 ────│
    │              │◀─ V1 사용자 목록 ────────────│                      │
    │◀─ 선택된 결재자 반환 ──│                    │                      │

[동명이인 구분 UX]: 부서명 앞 회사명 병기 (예: [V1] 인사팀 홍길동)
[성능]: coviTree Lazy Loading - 상위 부서 클릭 시 하위 노드 로드
```

---

### 2. 데이터 설계 (스키마/ERP, 캐시/저장 전략) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

#### 통합 조직도 데이터 구조 (기존 스키마 활용)

**`covi_syncdata.orgs_orgperson` (사용자):**

| 컬럼 | Key | REQ-ORG-003 활용 |
|---|---|---|
| UserCode | PK | 사용자 식별 |
| CompanyCode | PK | **V1 / V2 계열사 구분** |
| DeptCode | | 소속 부서 |
| LogonID | | 로그인ID (메일ID) |
| DisplayName | | 이름 (조직도 표시, 동명이인 시 [V1]/[V2] 병기) |
| JobPositionCode | | 직위 |
| PhotoPath | | 사진 (F-COR-SHR-002 연계) |
| MailAddress | | 메일 (타 계열사 노출 여부 정책 미확정) |
| IsUse | | Y: 재직, N: 퇴직 (조회 필터) |
| IsDisplay | | Y: 조직도 표시 대상 |
| SortKey | | 정렬 기준 |

**`covi_syncdata.orgs_orgdept` (부서):**

| 컬럼 | Key | REQ-ORG-003 활용 |
|---|---|---|
| GroupCode | PK | 부서 식별 |
| CompanyCode | PK | **V1 / V2 계열사 구분** |
| MemberOf | | 상위 부서 코드 (트리 구성) |
| DisplayName | | 부서명 |
| IsUse | | Y: 사용, N: 비사용 |
| IsDisplay | | Y: 조직도 표시 |

> **핵심**: 두 테이블 모두 `CompanyCode`가 PK에 포함되어 V1·V2 데이터가 이미 동일 테이블에서 공존하는 구조. 별도 통합 작업 불필요.

#### 계열사 노출 속성 정책 (미확정 → 기본 정의)

| 속성 | 기본 노출 (BASIC) | 확정 후 적용 |
|---|---|---|
| UserCode, CompanyCode | Y | Y |
| DisplayName (이름) | Y | Y |
| DeptCode / 부서명 | Y | Y |
| JobPositionCode / 직위 | Y | Y |
| PhotoPath (사진) | Y | Y |
| MailAddress (메일) | Y | 정책 확정 후 노출 여부 결정 |
| PhoneNumber, Mobile | N (기본 미노출) | 정책 확정 후 노출 여부 결정 |

#### 캐시/저장 전략

- **Redis 캐싱**: 통합 조직도 데이터를 Redis에 캐싱하여 반복 조회 시 DB 부하 최소화
- **Lazy Loading**: coviTree - 상위 부서 클릭 시 하위 노드 동적 로드 (대량 조회 성능 최적화)
- **`sys_base_config.IsGlobalOrgChart`**: Y 설정 시 도메인 필터 우회 전용 Mapper Query 적용
- **조회 전략**: `orgs_orgperson`, `orgs_orgdept`를 `CompanyCode` 기준으로 그룹화하여 통합 조회

---

### 3. 인터페이스 (API Specification) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

| 인터페이스 | 방향 | 방식 | IF ID | 설명 |
|---|---|---|---|---|
| 전자결재 → 공통 조직도 팝업 | IN | 화면 컴포넌트 호출 | IF-COV-USR-243 | `F-COR-SHR-001` CompanyCode=ALL 파라미터 전달 |
| 이음톡 → MobileOrgCon | IN | REST | IF-COV-USR-938 | `mobile/org/getSubList.do` CompanyCode 전체 조건 적용 |
| 포탈 → OrgManageCon | IN | REST | IF-COV-USR-938 | `admin/orgmanage/getInitOrgTreeList.do` CompanyCode=ALL |
| 포탈 → OrgApiCon (통합) | IN | REST | IF-COV-EXT-013 | `GET /covicore/api/org/getUnifiedList.do` 통합 조직도 |

---

### 4. 구현 가이드 (Pseudo Code / Logic) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

```
[통합 조직도 트리 조회 - 기존 API 파라미터 변경]

// 기존: OrgManageCon.getInitOrgTreeList.do
// AS-IS: CompanyCode = 현재 로그인 사용자 CompanyCode
// TO-BE: CompanyCode = '' 또는 'ALL' → 전체 회사 데이터 반환

PROCEDURE getInitOrgTreeList(CompanyCode):
  IF sys_base_config.IsGlobalOrgChart = 'Y':
    IF CompanyCode IS NULL OR CompanyCode = 'ALL':
      depts = SELECT * FROM orgs_orgdept WHERE IsUse='Y' AND IsDisplay='Y'
              ORDER BY CompanyCode, SortKey, DisplayName
    ELSE:
      depts = SELECT * FROM orgs_orgdept WHERE CompanyCode=? AND IsUse='Y'
              ORDER BY SortKey, DisplayName
  RETURN buildTree(depts)  // coviTree Lazy Loading 방식

[전자결재 결재선 선택 - 공통 팝업 파라미터 수정]

// AS-IS: 공통 팝업 호출 시 CompanyCode = 현재 사용자 CompanyCode
// TO-BE: CompanyCode = '' (전체) 로 변경
// 수정 위치: 전자결재 모듈 내 결재선 추가 버튼 호출부 (파라미터 1개 변경)

PROCEDURE openOrgSelectPopup(callerModule):
  IF callerModule IN ['approval', 'messenger']:
    companyCodeFilter = ''   // 전체 계열사 표시
  ELSE:
    companyCodeFilter = currentUser.companyCode  // 기존 동작 유지

[이음톡 대화 상대 검색 - MobileOrgCon 파라미터 변경]

// AS-IS: getSubList.do → CompanyCode = 현재 사용자 CompanyCode
// TO-BE: getSubList.do → CompanyCode = '' (전체)
// 동명이인 구분: DisplayName 앞 회사명 병기 (예: [V1] 홍길동)

PROCEDURE getSubList(keyword, companyCode):
  IF companyCode IS NULL OR companyCode = '':
    users = SELECT *, CONCAT('[', CompanyCode, '] ', DisplayName) AS DisplayName
            FROM orgs_orgperson
            WHERE IsUse='Y' AND IsDisplay='Y'
              AND (DisplayName LIKE '%{keyword}%' OR LogonID LIKE '%{keyword}%')
            ORDER BY CompanyCode, SortKey, DisplayName
  ELSE:
    [기존 로직]

[계열사 간 노출 속성 제어 - 정책 확정 후 적용]

PROCEDURE applyExposurePolicy(userVO, callerCompanyCode):
  IF userVO.companyCode != callerCompanyCode:
    // 타 계열사 임직원 → BASIC 속성만 노출
    MASK userVO.phoneNumber = null
    MASK userVO.mobile = null
    // 정책 확정 후 추가 마스킹 항목 적용 (별도 Filter 또는 AOP)
  RETURN userVO

// coviTree.js 확장 및 재활용
function initUnifiedTree() {
    $("#orgTree").coviTree({
        url: "/covicore/admin/orgmanage/getInitOrgTreeList.do",
        params: { CompanyCode: "ALL" },   // 전체 계열사
        lazyLoad: true,                   // 대량 조회 성능 최적화 (필수)
        onNodeClick: function(node) { loadChildNodes(node); }
    });
}
```

---

### 5. 패키지 구조 (레이어드 아키텍처 정의)

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

```
smarts4j.covicore
└── egovframework.core
    ├── org
    │   ├── controller
    │   │   ├── OrgManageCon.java         ← 기존 (재활용): getInitOrgTreeList, getUserList 등
    │   │   ├── OrgApiCon.java            ← 기존 (재활용): org/user/list, org/dept/list
    │   │   └── MobileOrgCon.java         ← 기존 (재활용): mobile/org/getSubList
    │   └── service
    │       ├── OrganizationManageSvc.java ← 기존 (재활용): 조직도 조회 비즈니스 로직
    │       └── OrgSvc.java               ← 기존 확장: 도메인 통합 쿼리 지원 (IsGlobalOrgChart)
    └── common
        └── service
            └── CommonSvc.java            ← 기존 (재활용): F-COR-SHR-001 조직도 팝업
                                                             F-COR-SHR-002 사용자 사진

[신규 개발 대상 - 최소화]
- 기존 API 파라미터 조건 변경 (CompanyCode 전체 조건 지원 보완)
  └── OrgManageCon.getInitOrgTreeList: CompanyCode=ALL 처리 분기 보완
  └── MobileOrgCon.getSubList: CompanyCode 빈 값 → 전체 사용자 검색 보완
- sys_base_config.IsGlobalOrgChart 옵션 추가 (통합 조직도 활성화 플래그)
- 통합 조직도 전용 Mapper Query 신규 개발 (도메인 필터 우회)
- 응답 필터링 레이어 (노출 정책 확정 후) - 별도 Filter 또는 AOP 방식 적용
- coviTree Lazy Loading 설정 적용 (필수)
```

---

### 6. Entity 설계 (JPA 엔티티 및 관계 매핑) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

> **기존 MyBatis 기반 시스템. 기존 VO 재활용.**

```java
// REQ-ORG-003 | DP-ORG-003 | DR-ORG-003
// 기존 OrgPersonVO 재활용 (REQ-ORG-001과 동일)
// CompanyCode 필드가 이미 PK에 포함되어 있어 V1/V2 구분 가능

// 노출 속성 제어를 위한 신규 VO (정책 확정 후 추가 개발)
public class OrgPersonExposureVO {
    private String userCode;
    private String companyCode;       // V1 / V2
    private String displayName;       // 기본 노출 (타 계열사 시 [CompanyCode] 병기)
    private String deptName;          // 기본 노출
    private String jobPositionName;   // 기본 노출
    private String photoPath;         // 기본 노출
    private String mailAddress;       // 정책 확정 후 노출 여부 결정
    private String phoneNumber;       // 정책 확정 전 기본 미노출 (타 계열사)
    private String mobile;            // 정책 확정 전 기본 미노출 (타 계열사)
}
```

---

### 7. DTO/VO (데이터 전송 객체 정의) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

```java
// 통합 조직도 트리 응답 VO (기존 OrgManageCon 응답 확장)
// REQ-ORG-003 | DP-ORG-003 | DR-ORG-003
public class IntegratedOrgTreeVO {
    private String companyCode;         // V1 / V2
    private String companyName;         // 계열사명
    private List<OrgDeptVO> depts;      // 부서 트리
}

// 사용자 검색 조건 VO (기존 getUserList 조건 확장)
public class OrgUserSearchVO {
    private String companyCode;         // null or '' = 전체, 'V1'/'V2' = 필터
    private String deptCode;
    private String keyword;             // 이름 / LogonID 검색
    private String isUse;              // 'Y' 고정 (재직자만)
}

// 계열사 목록 응답 VO (getavailablecompanylist 재활용)
public class CompanyListVO {
    private String companyCode;         // V1 / V2
    private String companyName;         // 계열사명
    private String isUse;              // Y/N
}
```

---

### 8. Service 로직 (핵심 메서드 시그니처) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

```java
// REQ-ORG-003 | DP-ORG-003 | DR-ORG-003
// 기존 OrganizationManageSvc 재활용 (파라미터 조건 변경)

public interface OrganizationManageSvc {
    // 기존 메서드 재활용 - CompanyCode='' 조건 추가 보완
    List<Map<String,Object>> getDeptList(Map<String,Object> params);     // CompanyCode 전체 지원
    Map<String,Object> getInitOrgTreeList(Map<String,Object> params);    // CompanyCode=ALL 지원
    List<Map<String,Object>> getUserList(Map<String,Object> params);     // CompanyCode 전체 지원
    List<Map<String,Object>> getavailablecompanylist(Map<String,Object> params); // 계열사 목록 (기존)
}

// OrgSvc (기존 확장)
public interface OrgSvc {
    // 통합 조직도 전용 (IsGlobalOrgChart=Y 시 도메인 필터 우회)
    List<Map<String,Object>> getUnifiedDeptList(Map<String,Object> params);
    List<Map<String,Object>> getUnifiedUserList(Map<String,Object> params);
}

// MobileOrgCon (기존 재활용)
// getSubList.do: CompanyCode 빈 값 → 전체 회사 사용자 검색 지원 보완
// 동명이인 구분: [CompanyCode] + DisplayName 형식으로 응답
```

---

### 9. API 명세 (REST API 정의) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

| Method | URI | 권한 | IF ID | 설명 | 변경 내용 |
|---|---|---|---|---|---|
| POST | `admin/orgmanage/getInitOrgTreeList.do` | ROLE_USER | IF-COV-USR-938 | 통합 조직도 트리 초기 조회 | **파라미터 조건 변경** (CompanyCode=ALL 지원) |
| POST | `admin/orgmanage/getdeptlist.do` | ROLE_USER | IF-COV-USR-941 | 부서 목록 조회 (전체 계열사) | **파라미터 조건 변경** |
| POST | `admin/orgmanage/getuserlist.do` | ROLE_USER | IF-COV-USR-940 | 사용자 목록 조회 (전체 계열사) | **파라미터 조건 변경** |
| POST | `admin/orgmanage/getavailablecompanylist.do` | ROLE_USER | IF-COV-USR-940 | 계열사 목록 조회 | **재활용 (변경 없음)** |
| POST | `mobile/org/getSubList.do` | ROLE_USER | IF-COV-USR-938 | 이음톡 대화 상대 검색 (전체 계열사) | **파라미터 조건 변경** |
| POST | `mobile/org/getOrgInfo.do` | ROLE_USER | IF-COV-USR-939 | 사용자/부서 상세 조직 정보 조회 | **재활용** |
| GET | `api/v1/org/getUnifiedList.do` | ROLE_USER | IF-COV-USR-290 | 통합 조직도 조회 (전체 계열사) | **신규** (LDAP/통합 수신처 패턴 재활용) |
| POST | `api/v1/org/user/list.do` | ROLE_USER | IF-COV-EXT-013 | 외부 연동용 사용자 목록 (CompanyCode 전체 지원) | **파라미터 조건 변경** |
| POST | `api/v1/org/dept/list.do` | ROLE_USER | IF-COV-EXT-018 | 외부 연동용 부서 목록 (CompanyCode 전체 지원) | **파라미터 조건 변경** |

---

### 10. 외부 연동 (타 시스템/모듈 연동) AI 설계 최종본

> 요구사항ID: REQ-ORG-003 | 설계계획ID: DP-ORG-003 | 설계보고ID: DR-ORG-003

| 연동 대상 | 연동 방식 | 방향 | 연동 내용 | IF ID | 비고 |
|---|---|---|---|---|---|
| 전자결재 모듈 | 공통 조직도 팝업 컴포넌트 | IN | 결재선 선택 시 공통 조직도 팝업 호출. CompanyCode=ALL 파라미터 적용 | IF-COV-USR-243 | 파라미터 1개 변경으로 대응 |
| 이음톡 모듈 (Mobile) | REST (내부) | IN | 대화 상대 검색 시 `MobileOrgCon.getSubList` 호출. CompanyCode 전체 조건 적용 | IF-COV-USR-938 | 파라미터 조건 변경, 동명이인 구분 UX 적용 |
| 포탈 UI | REST (내부) | IN | 포탈 조직도 화면 초기 로딩 시 `getInitOrgTreeList` 호출 (coviTree Lazy Loading) | IF-COV-USR-938 | CompanyCode=ALL 조건 |
| 사용자 사진 서비스 | 공통 서비스 | 내부 | 프로필 사진 스트리밍 (V1·V2 통합) | IF-COV-USR-385 | F-COR-SHR-002 재활용 |

---

## 산출 근거

| 출처 문서 | 참조 내용 |
|---|---|
| `300AI. 설계\300AI_D_030. 설계 수행(Dev)_개발자 의견, AI 정보 취합\조직도\claude\300AI_D_030.REQ-ORG-003_설계 수행보고서_202604141426_v1.0.md` | 아키텍처, 데이터 설계, GAP 분석, API 명세 (claude AI 설계) |
| `300AI. 설계\300AI_D_030. 설계 수행(Dev)_개발자 의견, AI 정보 취합\조직도\gemini\300AI_D_030.REQ-ORG-003_설계 수행보고서_202604141640_v1.1.md` | 개발자 의견, UnifiedOrgTree·coviTree 컴포넌트, Lazy Loading, 동명이인 UX, IsGlobalOrgChart 옵션 (gemini AI 설계) |
| `AI_Folder\smarts4j.covicore\docs\interfaces\IF_CORE_OrgManageCon.md` | `getInitOrgTreeList`, `getUserList`, `getavailablecompanylist` 기존 API 확인 (IF-COV-USR-938~941) |
| `AI_Folder\smarts4j.covicore\docs\interfaces\IF_CORE_OrgApiCon.md` | `org/user/list.do`(IF-COV-EXT-013), `org/dept/list.do`(IF-COV-EXT-018) CompanyCode 파라미터 확인 |
| `AI_Folder\smarts4j.covicore\docs\interfaces\IF_CORE_MobileOrgCon.md` | 이음톡 대화 상대 검색 `getSubList` 기존 구현 확인 |
| `AI_Folder\smarts4j.covicore\FUNCTIONAL_SPEC_CORE.md` | F-COR-SHR-001(조직도 팝업 IF-COV-USR-243), F-COR-SHR-002(사용자 사진 IF-COV-USR-385) 기존 구현 확인 |
| `AI_Folder\item\GW\Scheme.md` | `orgs_orgperson` PK=(UserCode, CompanyCode), `orgs_orgdept` PK=(GroupCode, CompanyCode) 다중 회사 구조 확인 |
