# [개발 상세 명세서] REQ-ORG-001 - 인사연동 (수동 관리 및 최초 이관)

## (1) 기본 정보
- **요구사항ID**: REQ-ORG-001
- **모듈(개발)별 개발요청 ID**: 400AI_E_010.REQ-ORG-001_개발 요청서 (상세 분석 대기 중)
- **모듈(개발)별 개발 계획 ID**: 400AI_E_020.REQ-ORG-001_개발 계획서_202604291900_v1.1
- **모듈(개발)별 개발 보고 ID**: 400AI_E_030.REQ-ORG-001_개발 수행 보고서_202604292000_v1.1
- **서브시스템**: ORG (조직도)
- **모듈(개발)별 코드**: smarts4j.covicore (조직도 관리 모듈)
- **개발**: 엑셀 업로드 기반 대량 이관 및 관리자 직접 관리 체계
- **개발자**: 이윤정
- **작업일**: 2026-04-29
- **최종 승인자**: PM 이상훈
- **최종 승인일**: 2026-04-29

## (2) 개발 프로그램 명세서 상세 내용

### 1. 데이터베이스 및 엔티티 설계 (REQ-ORG-001)
- **테이블 명세**:
    - `sys_object_user`: 엑셀 이관 데이터가 최종 저장되는 테이블.
    - `sys_object_group`: 엑셀 이관 부서 정보 저장 테이블.
- **컬럼 상세**:
    - `IsHR` (char(1)): 'N' (수동 관리 대상자임을 표시).
    - `SyncType` (varchar(10)): 'EXCEL' (최초 이관 방식 기록).
- **캐시 설계**:
    - 엑셀 업로드 성공 시 `RedisShardsUtil.flushCache("OrgsCache")` 즉시 실행.

### 2. 시스템 아키텍처 및 흐름 (REQ-ORG-001)
- **컴포넌트 다이어그램**:
    - `Excel Upload UI` -> `OrgManageCon` -> `Apache POI` -> `ValidationSvc` -> `MyBatis Bulk Insert`.
- **비즈니스 프로세스(BPM)**:
    1. 인사 담당자가 표준 엑셀 템플릿 작성.
    2. 관리자 페이지에서 파일 업로드.
    3. 데이터 유효성 검사 (사번 중복, 도메인 존재 여부).
    4. DB Bulk Insert 수행.
    5. AD 동기화 트리거 (REQ-ORG-002 연계).
- **상태 코드 정의**:
    - `UPLOAD_SUCCESS`: 업로드 완료.
    - `VALIDATION_ERROR`: 데이터 정합성 오류 (실패 행 리스트 반환).

### 3. API 및 인터페이스 명세 (REQ-ORG-001)
- **Endpoint**: `POST /covicore/admin/orgmanage/excelUpload.do`
- **Request Header/Body**:
    - Header: `Content-Type: multipart/form-data`
    - Body: `file: [Binary]`, `companyCode: "V1"`
- **필드별 Validation 체크**:
    - 사번: 필수, 중복 불가.
    - 부서코드: `sys_object_group`에 존재하는 코드여야 함.
- **Response Body**:
    - `{ "status": "SUCCESS", "successCount": 150, "failRows": [] }`

### 4. 핵심 로직 구현 상세 (REQ-ORG-001)
- **Service Layer 메서드**:
    - `processExcelUpload()`: POI 라이브러리를 이용한 행 단위 파싱.
    - `validateExcelRow()`: 비즈니스 룰 기반 정합성 체크.
- **트랜잭션 관리**:
    - `@Transactional` 적용하여 한 행이라도 정합성 오류 발생 시 전체 롤백(All-or-Nothing) 정책 권장.
- **비동기 처리**: 대량(500건 이상) 업로드 시 `Job` 형태로 백그라운드 실행 및 결과 알림.

### 5. 권한 및 보안 (REQ-ORG-001)
- **인가(Authorization)**: `ROLE_SYS_ADMIN` 권한만 엑셀 업로드 메뉴 접근 가능.
- **데이터 접근 권한**: 업로드 시 `companyCode` 파라미터를 강제하여 타사 데이터 오입력 방지.
- **개인정보 처리**: 업로드 파일은 처리 완료 즉시 임시 디렉토리에서 영구 삭제.

### 6. 테스트 케이스 명세 (REQ-ORG-001)
- **단위 테스트(Unit Test)**:
    - `testEmptyExcel()`: 빈 파일 업로드 시 예외 처리 확인.
    - `testInvalidDeptCode()`: 존재하지 않는 부서 코드 입력 시 반려 확인.
- **모킹(Mocking) 전략**: `MultipartFile` 객체를 모킹하여 다양한 파일 구조 테스트.

### 7. 인수 조건(AC) 검증 결과 (REQ-ORG-001)
- **대량 이관**: 200명 데이터 업로드 및 DB/AD 반영 확인 (Pass).
- **중복 체크**: 동일 사번 업로드 시 '중복 데이터' 에러 발생 확인 (Pass).
- **다국어 처리**: 베트남어 성명이 깨짐 없이 정상 저장됨 (Pass).

### 8. 영향도 평가 보고 (REQ-ORG-001)
- **영향 범위**: 조직도 초기 데이터 셋업 및 AD 계정 생성 인프라.
- **리스크**: 엑셀 양식 오기입으로 인한 데이터 오염 위험.
- **대응**: 업로드 전 '미리보기 및 검증' 단계 제공 및 DB 백업 선행 가이드.

---
**산출 근거**: 
1. `231.유티아이베트남_그룹웨어_시스템_구축_요구사항정의_및_GAP분석서_v1.6_20260429.xlsx`
2. `400AI_E_030.REQ-ORG-001_개발 수행 보고서_202604292000_v1.1.md`
