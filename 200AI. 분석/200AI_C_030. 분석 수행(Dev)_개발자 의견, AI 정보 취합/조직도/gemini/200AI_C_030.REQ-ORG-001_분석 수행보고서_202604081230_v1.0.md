# [Analysis Execution Report] REQ-ORG-001 - 인사연동

## (1) 기본 정보
- **요구사항 ID**: REQ-ORG-001
- **서브시스템**: 조직도 (ORG)
- **요구사항명**: 인사연동
- **요구사항**: V1: 수동, V2: 영림원 연계
- **요구사항 상세 설명**: 영림원 ERP(MSSQL)의 `GW_Department`, `GW_User` View를 그룹웨어(MySQL)와 연동하여 조직 정보를 자동 동기화합니다. V1은 수동 관리, V2는 자동 연동을 수행합니다.
- **요구사항 출처**: 고객 인터뷰 및 분석 요청서 (REQ-ORG-001)

## (2) 개발팀 확인 요청 내용
- **개발팀**: 솔루션 개발본부 / 기술지원팀
- **개발자(담당자)**: 이윤정
- **계획일(시작/종료일)**: 2026-04-08 ~ 2026-05-06
- **모듈(개발)별 분석 요청 ID**: REQ-ORG-001_ANALYSIS_REQ
- **매핑/GAP 분석**: 
    - **분석 결과**: MSSQL View(`dbo.GW_Department`, `dbo.GW_User`)와 Smarts4j 표준 테이블 간의 1:1 매핑 가능성 확인.
    - **GAP**: V1(수동)과 V2(자동)의 데이터 소스가 상이함에 따라, 동기화 배치 로직 내에서 도메인ID(DomainID)별 분기 처리가 필수적임.
- **GAP 대응 방안**: `HRConnectCode.java` 샘플 기반의 커스텀 배치 모듈을 개발하여 `CoviJobCon`(Batch Manager)에 등록. 배치 로직 내에서 V2 도메인에 대해서만 ERP DB 커넥션을 활성화하고, V1은 스킵(Skip) 처리.
- **분류**: 기능/인터페이스
- **수용여부**: 수용
- **비고**: ERP 측 View 보안 정책(접근 IP 제한) 및 MSSQL JDBC 드라이버 버전(MSSQL 2019 이상 권장) 확인 필요.

## (3) 분석 범위
- **영향도 분석**: 
    - **기존 모듈**: `smarts4j.covicore` 내의 `OrgApiCon`을 통한 데이터 업데이트 시 Redis 캐시(`covi_cache_org`) 강제 갱신 필요.
    - **DB**: 연동 이력 관리를 위한 `sys_interface_log` 테이블 활용.
    - **인프라**: MSSQL 서버와 GW 서버 간의 1433 포트 오픈 필수.
- **기술적 타당성**: 
    - **구현 난이도**: 중간. 표준 배치 프레임워크를 활용하므로 안정성 확보 용이.
    - **제약 사항**: 실시간 동기화가 아닌 스케줄 방식(매일 새벽 1시)이므로 데이터 지연 발생 가능.
- **리소스 추정**: 8 Man-Day
- **의존성 및 라이브러리 검토**: `mssql-jdbc` (Driver), `MyBatis` (Mapper), `Quartz` (Scheduler).
- **API 및 서비스 인터페이스 영향도**: 인사 연동 완료 후 `Common.js`의 `getBaseConfig`를 통해 클라이언트 사이드 설정 동기화 유도.
- **데이터 정합성 및 트랜잭션 전략**: 
    - **전략**: Interface Table(Staging)에 우선 적립 후, 유효성 검사(Validation)를 통과한 데이터만 본 테이블에 `Batch Insert/Update` 수행.
- **기술적 위험도 점수**: 3/5 (네트워크 단절 시 배치 실패 위험, 재시도 로직으로 보완 가능)

## (4) REQ-ORG-001_GAP분석 (개발 + AI 의견 취합)
- **개발팀 의견**: 영림원 ERP의 View 구조가 Smarts4j 표준과 유사하여 매핑 작업은 수월할 것으로 예상되나, 베트남 현지 네트워크 지연(Latency)으로 인해 대량 데이터 처리 시 타임아웃 설정(`socketTimeout`)을 넉넉히 가져가야 함.
- **AI 분석 의견**: `HRConnectCode.java`에서 확인된 `fetchRows` 방식은 소량 데이터에는 적합하나, 수천 명 단위의 사용자 동기화 시에는 `MyBatis ResultHandler`를 사용한 스트리밍 처리로 메모리 부하를 줄여야 함. 또한, 퇴사자 처리(IsUse='N') 시 결재 진행 중인 문서에 대한 대리결재자 지정 여부 등 비즈니스 로직 연계가 누락되지 않도록 주의 필요.

---
**산출 근거**:
- `AI_Folder\item\HR\HRConnectCode.java` 소스 분석 결과
- `AI_Folder\smarts4j.covicore\TECHNICAL_GUIDE_CORE.md` 아키텍처 가이드
- `200AI. 분석\210AI. 인터뷰\조직도\회의록_공통.md` 고객 협의 내용
