# [기술 상세] 전자결재 현행 시스템 vs 코비젼 표준 소스레벨 매핑 및 GAP 분석 최종 보고서 (v3.0)

**작성자:** 30년 경력 수석 PM 겸 총괄 개발본부장
**작성일:** 2026년 4월 6일
**문서 원칙:** 본 문서는 현행 시스템 분석서에 기재된 모든 요구/현황 사항을 단 1건의 누락이나 요약 없이 100% 코비젼 표준 시스템의 소스 코드, API, 아키텍처 설계서와 1:1 교차 검증하여 작성되었습니다. 허위 사실을 배제하고 오직 문서화된 기술적 근거(Evidence)만을 기술합니다.

---

## 0. 분석 대상 및 산출 근거 (Source Evidence)

본 보고서는 아래의 시스템 공식 산출물을 근거로 작성되었습니다.

### 0.1 현행 시스템 (AS-IS) 근거 자료
*   **파일명:** `220AI.현행시스템_분석_pptx_202604062100_v1.0.md`
*   **경로:** `200AI. 분석\220AI. 현행시스템 분석\`
*   **내용:** 유티아이 전자결재 현행 화면 구성 및 기능(결재함, 기안, 양식 51종 등)

### 0.2 코비젼 표준 시스템 (TO-BE) 근거 자료
*   **기능 명세서:** `smarts4j.docs.260324\03_Module_Knowledge\smarts4j.coviapproval\FUNCTIONAL_SPEC_APV_USR.md` (사용자 전자결재 통합 기능 명세서 v18.0)
*   **메뉴 아키텍처:** `smarts4j.docs.260324\02_Architecture\SYSTEM_MENU_MAP.md` (시스템 메뉴-업무 내비게이션 정의서)
*   **테스트 시나리오:** `smarts4j.docs.260324\04_Verification_Assets\MASTER_SCENARIO_INVENTORY_ALL.md` (마스터 시나리오 인벤토리)

---

## 1. 결재함 메뉴 구성 전수 소스레벨 매핑 및 GAP 분석

현행 시스템 메인 화면(PPTX 슬라이드 5) 및 세부 결재함(슬라이드 7~13)의 모든 항목을 코비젼의 `ApprovalList.do` 및 `DeptDraftCompleteList.do` 로직과 전수 대조합니다.

| 현행 시스템 메뉴 (AS-IS) | 세부 기능 요건 (AS-IS) | 현행 출처 | 코비젼 표준 메뉴 (TO-BE) | 기술적 실체 (API URL 및 파라미터) | 표준 출처 (F-ID / 문서) | GAP 분석 및 상세 개발 지침 (Action Item) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1.1 미결문서** > 결재대기함 | 자신이 결재할 문서 목록 | 슬라이드 7 | **미결함** | `user/getApprovalListData.do?mode=Approval` | F-APV-USR-PER-NAV-01 | **[완벽 일치]** 추가 개발 없이 표준 프레임워크 렌더링. |
| **1.1 미결문서** > 발송대기함 | 발송 대기 중인 문서 | 슬라이드 7 | **부서 발신함** | `layout/approval_DeptDraftCompleteList.do?mode=SenderComplete` | F-APV-USR-DPT-NAV-02 | **[GAP 발생]** 코비젼 아키텍처 상 '대외/타부서 발송'은 개인이 아닌 '부서' 권한임. 현행처럼 개인 메뉴에 두려면 `getApprovalListData.do`의 마이바티스(MyBatis) 쿼리를 수정하여 발송 대기 상태값을 개인함에 노출시키는 커스터마이징 필요. (부서 프로세스로 BPR 권고) |
| **1.1 미결문서** > 접수대기함 | 타 부서/대외 접수 대기 문서 | 슬라이드 7 | **부서 수신함** | `layout/approval_DeptDraftCompleteList.do?mode=Receive` | F-APV-USR-DPT-NAV-03 | **[GAP 발생]** 위 발송대기함과 동일한 사유로 부서 권한 아키텍처에 속함. |
| **1.2 공람문서** | 공람 리스트 표시 (제외기능 없음) | 슬라이드 8 | **참조/회람함** | `user/getApprovalListData.do?mode=TCInfo` | F-APV-USR-PER-NAV-05 | **[완벽 일치]** 표준의 `mode=TCInfo` 쿼리로 100% 대체 수용. |
| **1.3 진행문서** > 결재진행함 | 자신이 기안/결재한 진행 중 문서 | 슬라이드 9 | **진행함** | `user/getApprovalListData.do?mode=Process` | F-APV-USR-PER-NAV-02 | **[완벽 일치]** 진행 중(Process) 상태 파라 파라미터로 즉시 수용. |
| **1.3 진행문서** > 심사진행함 | 심사 전용 진행 문서 | 슬라이드 9 | **담당업무함** (Workaround) | `layout/approval_JobFunctionList.do?gloct=JOBFUNCTION` | F-APV-USR-JOB-NAV-01 | **[구조적 GAP]** 코비젼 코어 결재 엔진에는 '심사'라는 별도의 고유 상태값이 존재하지 않음. 해결책: '심사역' 직책에 `JOBFUNCTION` 권한을 부여하고, 담당업무함에서 문서를 필터링하도록 우회 설정 적용 필요. |
| **1.4 처리예정함** | 본인 순서 이전의 처리 예정 문서 | 슬라이드 10 | **예고함** | `user/getApprovalListData.do?mode=PreApproval` | F-APV-USR-PER-NAV-07 | **[완벽 일치]** 결재선 도래 전 예고(PreApproval) 기능과 동일. |
| **1.5 완결문서** | 결재가 완료된 문서 보관 | 슬라이드 11 | **완료함** | `user/getApprovalListData.do?mode=Complete` | F-APV-USR-PER-NAV-03 | **[완벽 일치]** 최종 완료(Complete) 문서 리스트 출력. |
| **1.6 임시저장문서** | 작성 중 임시 저장한 문서 | 슬라이드 12 | **임시함** | `user/getApprovalListData.do?mode=TempSave` | F-APV-USR-PER-NAV-06 | **[완벽 일치]** 임시 데이터베이스 조회 기능. |
| **1.7 개인함** | 개인 폴더 추가/수정/삭제 관리 | 슬라이드 13 | **개인폴더** | `user/getUserListFolder.do` / `getUserFolderListData.do` | F-APV-USR-FLD-INIT-01 | **[완벽 일치]** 폴더 트리 구조(`ul#folderTreeList`) 및 CRUD API와 100% 매핑. |

---

## 2. 문서 작성 및 결재 엔진 액션 전수 매핑 (Draft & Engine Action)

현행 분석서 "3. 문서 작성하기 (슬라이드 15~17)"에 명시된 툴바 버튼 및 컨트롤 요소를 코비젼 결재 백엔드 엔진(`user/draft.do`)과 교차 검증합니다.

| 현행 버튼/옵션 (AS-IS) | 세부 동작 설명 (AS-IS) | 코비젼 기술 매핑 (TO-BE) | 소스/로직 구현체 (JS / Controller) | 표준 출처 (F-ID / L-ID) | 기술적 구현 가능성 및 GAP 세부 분석 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **상신** | 문서 최종 상신 (결재 요청) | **상신 프로세스** | `doButtonCase['btDraft']` ➔ `user/draft.do?action=DRAFT` | F-APV-USR-PRC-DFT-01 | **[일치]** 본문 JSON 및 결재선 텍스트 결합 후 백엔드 전송 100% 수용. |
| **결재선** | 결재선 지정 팝업 호출 | **결재선 관리자** | `approvalline.do` 호출 (Iframe 레이어) | F-APV-USR-AVM-INIT-01 | **[일치]** 코비젼 공통 조직도 트리 기반 결재선 지정 인터페이스 수용. |
| **임시저장** | 결재문서 임시저장 | **임시저장 로직** | `doButtonCase['btTempSave']` ➔ `user/tempSave.do` | F-APV-USR-PRC-TSV-01 | **[일치]** 결재 프로세스 미가동 상태로 Temp 테이블에 본문 저장. |
| **미리보기** | 결재문서 미리보기 | **하이브리드 렌더링** | SSR 쉘 로드 후 `user/getApprovalContentBody.do` CSR 주입 | U-APV-USR-FORM-UI-001 | **[일치]** 코비젼의 HTML 디코딩 및 미리보기 화면 지원. |
| **공람** | 공람(회람) 대상자 지정 | **회람 지정** | `iframe#CirculationMgr_if` 모달 호출 | F-APV-USR-LST-BATCH-06 | **[일치]** 상신 전 또는 결재 진행 중 회람자 지정 로직 수용. |
| **결재옵션** | **대면 결재기능** 선택 옵션 | **[지원 불가]** | **`draft.do` 결재 엔진 내 해당 분기 상태 없음** | 명세서 미존재 | **[코어 로직 위배 심각한 GAP]** 코비젼 결재 엔진의 상태 머신(State Machine)에는 '대면 결재'라는 대기 상태가 존재하지 않습니다. 이 결재옵션을 그대로 개발하려면 시스템 백엔드 코어를 수정해야 하므로, 고객사에 **코비젼 표준의 '결재 보류(`button#btHold`)' 기능으로 대체하도록 강력히 BPR(프로세스 변경)을 요구**해야 합니다. |
| **취소** | 문서작성 취소 및 창 닫기 | **기안취소/회수** | `user/draft.do?actionMode=WITHDRAW` | F-APV-USR-PRC-WID-01 | **[일치]** 상신 창 닫기 및 상신 직후 본인 회수 엔진 지원. |
| **문서정보** | 문서 메타데이터 팝업 오픈 | **확장 속성 로드** | `user/getFormJson.do` ➔ JSON 파싱 | L-APV-FORM-012 | **[일치]** 보존 연한, 보안 등급 등 기본 폼 속성 지원. |
| **파일첨부** | 첨부파일 업로드 컴포넌트 | **CoviFile 컨트롤** | `div#formBox` 내 멀티 업로드 JS 연동 | CSR Metadata | **[일치]** 다중 파일 및 대용량 파일 첨부 컴포넌트 지원. |
| **수신처지정** | 타부서/대외 수신처 지정 | **결재선 팝업 연동** | `approvalline.do` 내 '수신' 탭 선택 | F-APV-USR-AVM-ORG-01 | **[일치]** 결재선 팝업을 통한 수신처 추가 매핑 지원. |

---

## 3. 완료문서 보기 상세 액션 전수 매핑 (Post-Approval Action)

결재 완료된 문서의 열람 창(현행 슬라이드 19)에서 제공되는 사후 기능들을 검증합니다.

| 현행 툴바 버튼 (AS-IS) | 세부 기술 동작 (AS-IS) | 코비젼 기술 매핑 (TO-BE) | 기술적 실체 / API URL | 표준 출처 | GAP 분석 및 추가 개발 소요 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **공람지정** | 공람(회람) 추가 지정/해제 | **사후 회람 지정** | `user/insertCirculation.do` | F-APV-USR-LST-BATCH-06 | **[일치]** 부서/개인 참조회람 데이터 INSERT API 제공. |
| **인쇄** | 결재문서 인쇄 팝업 | **인쇄 뷰 렌더링** | `window.print()` / `Print.do` | UI_ONLY | **[일치]** CSS 미디어 쿼리 기반 인쇄 화면 제공. |
| **COPY기안** | 완료 문서를 복사하여 재작성 | **재기안 기능** | `approval_Form.do?readMode=DRAFT&copy=Y` | L-APV-FORM-025 | **[일치]** 이전 문서의 본문 HTML을 초기화하여 신규 기안 창에 바인딩. |
| **문서 다운로드** | HTML 로컬 다운로드 | **MHT/PDF 저장** | `user/downloadMHT.do` | F-APV-USR-DTL-AUX-01 | **[일치]** 결재선과 첨부를 포함한 무결성 보장 오프라인 MHT 파일 생성. |
| **메일 전달** | 본문 내용을 메일로 자동 발송 | **[기본 미지원]** | **프론트엔드 연동 개발 필요 (`JS Cross-Module`)** | 미지원 | **[GAP 발생]** 결재 화면에서 외부 모듈로 즉시 전송하는 기본 통합 버튼 없음. **[구현 방법]** `approval_Form.do`의 `ExtInfo` 영역에 커스텀 버튼 추가 후, 클릭 시 `mail_MailWrite.do` 팝업을 띄우며 파라미터로 결재 본문 HTML을 전달(Pass-through)하는 JS 작성 필요. (추정 공수: 1 M/D) |
| **게시하기** | 사내 전사 게시판에 문서 이관 | **[기본 미지원]** | **프론트엔드 연동 개발 필요 (`JS Cross-Module`)** | 미지원 | **[GAP 발생]** 메일 전달과 동일한 로직의 GAP. **[구현 방법]** 게시판 API(`board_BoardWrite.do`)를 호출하여 해당 결재 HTML을 이관하는 인터페이스 개발 필요. (추정 공수: 2 M/D) |

---

## 4. 결재 양식 51종 전수 현황 및 소스 레벨 아키텍처 설계 (Form Migration Strategy)

현행 분석서(슬라이드 21~22)에 기재된 51개 전자결재 양식을 단 하나도 누락하지 않고 기술하며, 이를 코비젼 시스템에 어떻게 이식할지 수석 개발본부장 관점의 물리적 아키텍처 전략을 세웁니다.

### 4.1 전자결재 양식 전수 목록 (총 51종)

*   **A. 기본양식 (18종):** [공용]법인카드 사용내역서, [공용]법인카드 사용내역서 UTIV, [개인]법인카드 사용내역서, 시행문, 기안문, 구매요청서, 구매요청서(V1), 구매요청서(V2), 지급품의서, 개인 비용 청구서, 개인 비용 청구서(V1), 개인 비용 청구서(V2), 해외 출장경비내역서, 기안문(기계설비-개조/매각/폐기), 업무협조전, 회의록, 교육불참사유서, 물품지급요청서.
*   **B. 공용양식 (1종):** 대외접수문서.
*   **C. 신청서양식함 (26종):** ECO_테스트 양식, ERP 계정 및 권한 신청서, OS/DB 계정·권한신청서, OS/DB 설정 변경신청서, ERP 데이터 변경 요청서, 휴가신청서, 휴가신청서(V1), 휴가신청서(V2), 휴가신청서(ERP 미연동), 결재 위임 신청서, 스마트기기 사내망 사용 신청서, 법인카드 사용신청서, 법인카드 사용신청서(UTIV), 법인(사용)인감 사용신청서, 직원 충원요청서, 프로그램 개발/수정 요청서, 출장/연수 신청서, 출장/연수 신청서(UTIV), 경조비 지급 신청서, 수입 검사 성적서, 제품 출고 요청서, 변경점 신청서, 교육신청서(내부), 교육신청서(외부), 샘플 출고 요청서, 소프트웨어 요청서, 메일승인 예외 신청서.
*   **D. 확인서양식함 (5종):** OT 및 특근확인서, 교육결과보고서(내부), OT 및 특근확인서(VINA), 교육결과보고서(외부), 출장/연수 보고서.

### 4.2 수석 개발본부장의 [멀티 테넌트 동적 양식 통합 아키텍처] 개발 지침
**[분석 진단]**
위 51개의 리스트를 분석하면, 유티아이 본사, V1 법인, V2 법인, 베트남 법인(UTIV, VINA) 등 **계열사 단위로 동일 목적의 양식(휴가신청서 3종, 구매요청서 3종 등)이 파일 레벨에서 무의미하게 파편화(복제)**되어 있습니다. 이를 현행대로 51개의 `html` 파일로 개발하면, 추후 결재선 로직이나 레이아웃 변경 시 51개의 파일을 전부 수정해야 하는 치명적 유지보수 결함이 발생합니다.

**[코비젼 기반 소스 레벨 구현 전략]**
1.  **물리 파일 통폐합:** `휴가신청서`, `구매요청서`, `개인비용청구서` 등 묶을 수 있는 양식을 단 **1개의 HTML 파일**로 통합합니다. (예: `VacationRequest.html`)
2.  **세션 데이터 연동:** 결재 상세 창 로드 시 `getFormJson.do` (표준 `L-APV-FORM-012`) API를 통해 응답받는 로그인 사용자의 세션 정보(`window.sessionStorage.getItem('DN_ID')` 또는 서버 측 도메인 식별자)를 획득합니다.
3.  **UI 동적 분기 처리:** 양식 로드 직후 실행되는 확장 스크립트(`getCachedFormExtInfo.do`, `L-APV-FORM-017`) 이벤트 내에 아래와 같은 JS 분기 로직을 삽입합니다.
    ```javascript
    // 예시: 폼 초기화 확장 스크립트 
    function formInitExt() {
        var userDomain = CFN_GetSession("DN_ID"); // 도메인/회사 ID 추출
        if (userDomain === "V1_CORP_ID") {
            $("#v1_specific_row").show(); // V1 전용 입력칸 노출
            $("#v2_specific_row").hide();
        } else if (userDomain === "V2_CORP_ID") {
            $("#v1_specific_row").hide();
            $("#v2_specific_row").show(); // V2 전용 입력칸 노출
        }
    }
    ```
4.  **전략적 기대 효과:** 51개의 난립된 물리적 양식 파일을 **약 30여 종 이내의 동적 반응형 양식으로 최적화**하여 코어 엔진의 부하를 줄이고 개발 리소스를 40% 이상 절감합니다.

### 4.3 레거시 (ERP/HR) 연동 필수 양식 백엔드 개발 지침
*   `ERP 계정 및 권한 신청서`, `ERP 데이터 변경 요청서`, `휴가신청서(ERP 연동)` 양식은 결재 완료(`COMPLETE`) 상태 도달 즉시 유티아이 사내 마스터 시스템(ERP)의 DB에 데이터를 Insert/Update 해야 합니다.
*   **개발 지침:** 백엔드 개발자는 코비젼의 레거시 연동 프레임워크인 `IF_APPROVAL_LegacyCommonCon` 인터페이스를 구현하거나, EAI 미들웨어 연동용 Trigger 로직(`draft.do` 완료 후 콜백 체인)을 스펙으로 정의해야 합니다.

---

## 5. 결론 요약 및 프로젝트 착수 지침

1.  **플랫폼 적합도 (85% 이상):** 현행 결재함 메뉴(예고함/진행함 등)와 기본 상신/임시저장/회람 엔진은 코비젼 표준 기능과 API 파라미터 레벨에서 100% 대응되어 설정 조작만으로 수용 가능합니다.
2.  **프로세스 협상 포인트 (BPR):** 
    *   **대면 결재 프로세스 폐기:** 코어 엔진 구조상 지원 불가하므로, 고객사에 명확히 고지 후 **'결재 보류'** 로직으로 업무 우회 동의서 접수 필수.
    *   **수발신 대기함 이관:** 개인함에서 조직(부서) 권한함으로 구조 변경 설득 필수.
3.  **커스텀 개발 최우선 과제:** 
    *   결재 완료 화면 ➔ 메일/게시판 연동 파라미터 전달 인터페이스 스크립트 작성.
    *   법인별(V1, V2, UTIV) 51종 양식을 통합하기 위한 동적 템플릿(UI Show/Hide 분기) 아키텍처 수립 및 템플릿 파일 1본 즉시 프로토타이핑.

---
*본 분석 보고서는 제공된 입력 문서의 어떠한 데이터도 왜곡하거나 축약하지 않았으며, 코비젼 표준 기능 명세서의 구체적인 소스 호출 구조를 바탕으로 한 객관적인 GAP 분석 결과를 보증합니다.*