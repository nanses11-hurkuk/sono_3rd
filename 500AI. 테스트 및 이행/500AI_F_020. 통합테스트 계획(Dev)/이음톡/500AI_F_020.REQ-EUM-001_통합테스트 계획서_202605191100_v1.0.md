# [통합테스트 계획서] REQ-EUM-001

> ※ 메신저(이음톡)는 소스 수정이 불가하여 간단한 시나리오 형식을 전제로 작성합니다.

---

## (1) 기본 정보

| 항목 | 내용 |
|------|------|
| **요구사항 ID** | REQ-EUM-001 |
| **서브시스템** | 이음톡 |
| **요구사항명** | 외부 사용자 |
| **요구사항 상세** | vendor 사에서도 사용할 수 있도록 요청 |
| **통합테스트 요청서 ID** | 500AI_F_010.REQ-EUM-001_통합테스트 요청서_202605131100_v1.0 |
| **통합테스트 계획서 ID** | 500AI_F_020.REQ-EUM-001_통합테스트 계획서_202605191100_v1.0 |
| **산출 근거** | - `400AI_E_130.REQ-EUM-001_단위테스트 완료 PM 승인서_202605131000_v1.0` |
| | - `300AI_D_040.REQ-EUM-001_설계 상세 결과서_202604151530_v1.0` |
| | - `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분석서_v1.5_20260414` (REQ-EUM-001) |

---

## (2) 통합테스트 계획 범위

### 2-1. 통합테스트 목적 및 범위

| 항목 | 내용 |
|------|------|
| **목적** | 외부 vendor 사용자의 이음톡 접근 인증 흐름(VendorAuthPlugin → JWT 발행 → WebSocket 세션 → 채널 권한 필터) 통합 동작 검증 |
| **범위** | `POST /api/eum/auth/external` → VendorAuthPlugin → EUM_VENDOR_USER_MAP → Redis ACL(TTL 2h) → WebSocket 채널 접근 전 구간 |
| **제외** | 소스 수정 불가로 내부 코드 레벨 단위 검증 제외 — 외부 I/F 및 기능 동작 확인 중심 |

---

### 2-2. 난이도

> **중** — VendorAuthPlugin 외부 API 검증 + JWT 발행 + Redis ACL TTL 관리 + WebSocket 권한 필터 연동. 소스 수정 불가로 설정·환경 중심 검증.

---

### 2-3. 개발자(담당자) 배정

| 역할 | 배정 요청 |
|------|---------|
| 통합테스트 주담당 | 단위테스트 담당 개발자 (이음톡 모듈) |
| QA | QA 파트 1인 배정 요청 |
| 협조 | vendor 테스트 계정 및 외부 API 검증 환경 구성 (PM + 인프라팀) |

---

### 2-4. 계획일(시작/종료일)

| 항목 | 일정 |
|------|------|
| **시작일** | 2026-05-18 |
| **종료일** | 2026-05-22 |
| **비고** | EUM_VENDOR_USER_MAP 테이블 및 테스트 vendor 계정 사전 구성 필요 |

---

### 2-5. 통합테스트 가능 여부

> **필수** — 단위테스트 PM 승인 완료. 아래 사전 조건 충족 후 진입

| 사전 조건 | 상태 |
|---------|------|
| EUM_VENDOR_USER_MAP 테이블 스테이징 환경 반영 | 확인 필요 |
| 외부 vendor API 검증 엔드포인트 Mock 또는 실 환경 준비 | 확인 필요 |
| Redis ACL TTL 설정 (2h) 스테이징 적용 | 확인 필요 |

---

### 2-6. 통합테스트 환경 및 인프라 구축 계획

| 항목 | 내용 |
|------|------|
| **테스트 서버** | 이음톡 스테이징 환경 (Dev 통합) |
| **외부 vendor API** | WireMock 또는 실 vendor API 엔드포인트 |
| **Redis** | ACL 세션 정보 TTL 2h 관리 인스턴스 |
| **DB** | EUM_VENDOR_USER_MAP 테이블 포함 스테이징 DB |
| **WebSocket** | 이음톡 WebSocket 서버 — 채널 권한 필터 동작 확인용 |

---

### 2-7. 통합테스트 데이터 전략

| 구분 | 내용 |
|------|------|
| **테스트 계정** | vendor 외부 사용자 계정 3종 (정상·비정상·만료) |
| **매핑 데이터** | EUM_VENDOR_USER_MAP — vendorId / extUserId 매핑 데이터 사전 입력 |
| **JWT 토큰** | 정상 발행 토큰, 만료 토큰, 잘못된 서명 토큰 3종 준비 |
| **초기화** | 각 시나리오 실행 전 Redis ACL 및 WebSocket 세션 초기화 |

---

### 2-8. 통합테스트 상세 범위 계획

| 통합 레이어 | 검증 대상 | 주요 확인 항목 |
|------------|---------|-------------|
| **UI ↔ API Layer** | 클라이언트 → `POST /api/eum/auth/external` | 요청 파라미터(vendorId, extUserId, secret) 정상 전달, JWT + WebSocket URL 응답 |
| **Service ↔ Service** | VendorAuthPlugin → 외부 Vendor API → Smarts4j 세션 Context 주입 | 외부 API 검증 성공/실패 분기, 채팅 접근 권한 체크 |
| **App ↔ Database** | VendorAuthPlugin → EUM_VENDOR_USER_MAP 조회 | 매핑 정보 정상 조회, 미등록 vendor 오류 응답 |
| **App ↔ Redis** | 세션 ACL Redis 저장 (TTL 2h) | ACL 정상 저장, TTL 만료 후 재인증 요구 |
| **Async Tasks** | WebSocket 세션 만료 후 자동 재연결 | 해당 설계 단계에서 협의 및 정의 필요 |

---

### 2-9. AI 기반 통합테스트 시나리오 설계 계획

| 케이스 ID | 시나리오 ID | 핵심 검증 내용 | 페르소나 | 통과 기준 | 멀티 유저 | 영향도 |
|----------|------------|-------------|--------|---------|---------|------|
| TC-EUM-001-INT-001 | SCN-001 | 정상 vendor 계정 인증 → JWT 발행 → WebSocket 접속 | Vendor 외부 사용자 | JWT 정상 발행, WebSocket 연결 성공 | 3명 동시 인증 | 높음 |
| TC-EUM-001-INT-002 | SCN-002 | 미등록 vendor 계정 인증 시도 → 오류 응답 | 미등록 Vendor 사용자 | 401 오류 응답, WebSocket 접속 차단 | — | 중 |
| TC-EUM-001-INT-003 | SCN-003 | Redis ACL TTL 만료 후 채팅 채널 재접근 → 재인증 요구 | Vendor 외부 사용자 | TTL 만료 감지, 재인증 흐름 정상 동작 | — | 중 |
| TC-EUM-001-INT-004 | SCN-004 | 잘못된 secret으로 인증 시도 → 권한 차단 | 비정상 Vendor 사용자 | 인증 실패 응답, 채널 접근 0건 | 3명 동시 | 중 |

---

### 2-10. AI 기반 통합테스트 실행 전략 계획

| 전략 | 내용 |
|------|------|
| **시나리오 합성** | SCN-001 + SCN-003 조합(정상 인증 → TTL 만료 → 재인증) End-to-End 복합 시나리오 |
| **스크립트 자율 생성** | Playwright 기반 — `/api/eum/auth/external` 요청·응답 및 WebSocket 연결 흐름 자동 생성 |
| **자가 치유(Self-Healing)** | API 응답 구조 변경 시 파서 자동 수정 |

---

### 2-11. 기본 코드 녹화(Recording) 계획

| 항목 | 내용 |
|------|------|
| **도구** | Playwright Codegen |
| **대상** | vendor 인증 → JWT 수신 → WebSocket 연결 → 채팅 채널 진입 흐름 |
| **산출물** | 녹화 스크립트 원본 → POM 리팩토링 입력 소재 |

---

### 2-12. AI를 이용한 스크립트 고도화(Refactoring to POM) 계획

| 항목 | 내용 |
|------|------|
| **POM 분리** | `VendorAuthPage`(인증 요청), `ChatChannelPage`(채널 접근) |
| **AI 역할** | 녹화 스크립트 → POM 클래스 자동 분리, 파라미터화 |

---

### 2-13. AI 데이터 모킹 및 네트워크 인터셉션(Data Mocking) 계획

| 항목 | 내용 |
|------|------|
| **Vendor API 모킹** | WireMock — 정상 응답, 오류, 타임아웃 시뮬레이션 |
| **Redis TTL 모킹** | 단기 TTL 설정으로 ACL 만료 시나리오 강제 실행 |
| **네트워크 인터셉션** | Playwright `route()` — `/api/eum/auth/external` 응답 지연·오류 주입 |

---

### 2-14. 검증 및 디버깅(Local Execution & Debug) 계획

| 항목 | 내용 |
|------|------|
| **로컬 실행** | Docker Compose (이음톡 + Redis + WireMock Vendor API + 스테이징 DB) |
| **디버깅 포인트** | VendorAuthPlugin 검증 분기, Redis ACL 저장·TTL 확인, WebSocket 권한 필터 |
| **로그** | `DEBUG` 레벨 — 인증 요청·JWT 발행·ACL 저장 전체 로깅 |

---

### 2-15. CI/CD 파이프라인 통합 및 모니터링(CI Integration) 계획

| 항목 | 내용 |
|------|------|
| **CI 도구** | Jenkins / GitHub Actions (프로젝트 표준) |
| **트리거** | 이음톡 모듈 소스 변경 시 자동 실행 |
| **알림** | 실패 시 담당 개발자 + QA 알림 |

---

### 2-16. 성공 지표 및 대시보드 계획

| 지표 | 목표 |
|------|------|
| 통합테스트 케이스 통과율 | 95% 이상 |
| 정상 vendor 인증 성공률 | 100% |
| Redis ACL TTL 만료 후 재인증 정상 동작 | 100% |
| 비정상 vendor 차단 | 0건 미통과 |

---

### 2-17. 이슈/리스크 예상 정보

| 번호 | 구분 | 내용 | 조치 방향 |
|------|------|------|---------|
| 1 | 🟡 중위험 | 외부 Vendor API 실 환경 미구성 → WireMock 대체 필요 | WireMock으로 인증 흐름 부분 검증 후 실 연동 재수행 |
| 2 | 🟡 중위험 | EUM_VENDOR_USER_MAP 테이블 스테이징 미반영 시 매핑 조회 실패 | 사전 데이터 입력 스크립트 준비 |
| 3 | 🟢 저위험 | Redis TTL 단축 설정으로 스테이징 검증 — 운영 TTL 복원 필요 | Staging 이관 시 TTL 원복 확인 |

---

### 2-18. 특이사항

- 소스 수정 불가 전제 — 설정 파일·환경 변수·DB 데이터 기반 검증에 집중
- `VendorAuthPlugin` 동작은 Smarts4j `CustomAuthenticator` 인터페이스 구현체 기준 — 플러그인 배포 여부 사전 확인 필요

---

**산출 근거**
- `500AI_F_010.REQ-EUM-001_통합테스트 요청서_202605131100_v1.0`
- `300AI_D_040.REQ-EUM-001_설계 상세 결과서_202604151530_v1.0`
- `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분析서_v1.5_20260414` (REQ-EUM-001)
