# [통합테스트 계획서] REQ-EUM-003

> ※ 메신저(이음톡)는 소스 수정이 불가하여 간단한 시나리오 형식을 전제로 작성합니다.

---

## (1) 기본 정보

| 항목 | 내용 |
|------|------|
| **요구사항 ID** | REQ-EUM-003 |
| **서브시스템** | 이음톡 |
| **요구사항명** | 다국어 |
| **요구사항 상세** | 베트남어 적용 |
| **통합테스트 요청서 ID** | 500AI_F_010.REQ-EUM-003_통합테스트 요청서_202605131100_v1.0 |
| **통합테스트 계획서 ID** | 500AI_F_020.REQ-EUM-003_통합테스트 계획서_202605191100_v1.0 |
| **산출 근거** | - `400AI_E_130.REQ-EUM-003_단위테스트 완료 PM 승인서_202605131000_v1.0` |
| | - `300AI_D_040.REQ-EUM-003_설계 상세 결과서_202604151530_v1.0` |
| | - `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분析서_v1.5_20260414` (REQ-EUM-003) |

---

## (2) 통합테스트 계획 범위

### 2-1. 통합테스트 목적 및 범위

| 항목 | 내용 |
|------|------|
| **목적** | 이음톡 베트남어(`vi`) 로케일 적용 — `ResourceBundle` 연동, `SYS_MULTILANGUAGE` 베트남어 데이터 반영, UI 렌더링(Noto Sans VI 폰트) 전 구간 통합 검증 |
| **범위** | 브라우저 로케일 감지(`Accept-Language: vi`) → `GET /api/common/i18n/messages?lang=vi` → 베트남어 사전 반환 → UI 키-밸류 렌더링 전 구간 |
| **제외** | 소스 수정 불가로 내부 i18n 프레임워크 코드 레벨 검증 제외 — UI 렌더링·API 응답 데이터 정합성 확인 중심 |

---

### 2-2. 난이도

> **중** — `SYS_MULTILANGUAGE` 베트남어 컬럼 데이터 입력 + Noto Sans VI 웹폰트 CDN 적용 + 성조 표현 렌더링. 소스 수정 불가로 설정·데이터 기반 검증.

---

### 2-3. 개발자(담당자) 배정

| 역할 | 배정 요청 |
|------|---------|
| 통합테스트 주담당 | 단위테스트 담당 개발자 (이음톡 모듈) |
| QA | QA 파트 1인 배정 요청 (베트남어 원어민 검수 권장) |
| 협조 | `SYS_MULTILANGUAGE` 베트남어 데이터 입력 담당 |

---

### 2-4. 계획일(시작/종료일)

| 항목 | 일정 |
|------|------|
| **시작일** | 2026-05-18 |
| **종료일** | 2026-05-22 |
| **비고** | `SYS_MULTILANGUAGE` 베트남어 컬럼 데이터 사전 입력 완료 필요 |

---

### 2-5. 통합테스트 가능 여부

> **필수** — 단위테스트 PM 승인 완료. 아래 사전 조건 충족 후 진입

| 사전 조건 | 상태 |
|---------|------|
| `SYS_MULTILANGUAGE` 베트남어(`vi`) 컬럼 데이터 입력 완료 | 확인 필요 |
| Noto Sans VI 웹폰트 CDN 경로 설정 완료 | 확인 필요 |
| `ResourceBundle` `vi` 로케일 파일 배포 완료 | 확인 필요 |

---

### 2-6. 통합테스트 환경 및 인프라 구축 계획

| 항목 | 내용 |
|------|------|
| **테스트 서버** | 이음톡 스테이징 환경 |
| **브라우저** | Chrome / Edge — `Accept-Language: vi` 헤더 강제 설정 |
| **DB** | `SYS_MULTILANGUAGE` 베트남어 데이터 반영 스테이징 DB |
| **CDN** | Noto Sans VI 웹폰트 CDN 경로 접근 가능 여부 확인 |
| **세션** | 사용자 설정 로케일 `vi` 세션 저장 동작 확인 |

---

### 2-7. 통합테스트 데이터 전략

| 구분 | 내용 |
|------|------|
| **다국어 데이터** | `SYS_MULTILANGUAGE` — 이음톡 UI 전체 키 베트남어 번역 데이터 입력 |
| **테스트 계정** | 로케일 설정 `vi` 계정, 로케일 미설정 계정(Accept-Language 기반 감지) |
| **폰트 검증** | 베트남어 성조 문자(à, ả, ã, á, ạ, ă 등) 포함 테스트 문자열 준비 |
| **초기화** | 각 시나리오 실행 전 세션 로케일 초기화 |

---

### 2-8. 통합테스트 상세 범위 계획

| 통합 레이어 | 검증 대상 | 주요 확인 항목 |
|------------|---------|-------------|
| **UI ↔ API Layer** | 브라우저 → `GET /api/common/i18n/messages?lang=vi` | 베트남어 키-밸류 JSON 정상 반환, 누락 키 0건 |
| **Service ↔ Service** | i18n API → `SYS_MULTILANGUAGE` 조회 → JSON 응답 조립 | 베트남어 데이터 정합성, 한국어/영어 로케일 대비 오버라이드 없음 |
| **App ↔ Database** | `GET /api/common/i18n/messages` → `SYS_MULTILANGUAGE` DB 조회 | 베트남어 컬럼 누락 키 0건, UTF-8 인코딩 정합성 |
| **Async Tasks** | CDN 폰트 로딩 → UI 성조 렌더링 | Noto Sans VI 폰트 정상 로딩, 성조 문자 깨짐 0건 |

---

### 2-9. AI 기반 통합테스트 시나리오 설계 계획

| 케이스 ID | 시나리오 ID | 핵심 검증 내용 | 페르소나 | 통과 기준 | 멀티 유저 | 영향도 |
|----------|------------|-------------|--------|---------|---------|------|
| TC-EUM-003-INT-001 | SCN-001 | `Accept-Language: vi` 헤더 기반 자동 로케일 감지 → 베트남어 UI 렌더링 | V2 베트남 현지 사용자 | 전체 UI 베트남어 표시, 누락 키 미발생 | — | 높음 |
| TC-EUM-003-INT-002 | SCN-002 | 사용자 설정 로케일 `vi` 세션 저장 → 재접속 시 베트남어 유지 | V2 베트남 현지 사용자 | 로그인 재접속 후 베트남어 UI 유지 | — | 중 |
| TC-EUM-003-INT-003 | SCN-003 | `/api/common/i18n/messages?lang=vi` 응답 JSON 키-밸류 검증 | 시스템(API 검증) | 전체 키 베트남어 번역 포함, 응답 2초 이내 | — | 높음 |
| TC-EUM-003-INT-004 | SCN-004 | 베트남어 성조 문자 UI 렌더링 (Noto Sans VI 폰트) | V2 베트남 현지 사용자 | 성조 문자 깨짐 0건, 폰트 정상 로딩 | V1/V2 혼합 3명 | 중 |
| TC-EUM-003-INT-005 | SCN-005 | 한국어 ↔ 베트남어 로케일 전환 | V1 한국어 사용자 / V2 베트남어 사용자 동시 접속 | 각 로케일 독립 표시, 상호 오버라이드 0건 | 2명 동시 | 중 |

---

### 2-10. AI 기반 통합테스트 실행 전략 계획

| 전략 | 내용 |
|------|------|
| **시나리오 합성** | SCN-001 + SCN-004 조합(자동 감지 → 성조 렌더링 확인) 복합 시나리오 |
| **스크립트 자율 생성** | Playwright — `lang=vi` API 요청·응답 JSON 파싱 및 UI 텍스트 검증 자동 생성 |
| **자가 치유(Self-Healing)** | UI 텍스트 셀렉터 변경 시 로케일 키 기반 자동 재매핑 |

---

### 2-11. 기본 코드 녹화(Recording) 계획

| 항목 | 내용 |
|------|------|
| **도구** | Playwright Codegen |
| **대상** | 베트남어 로케일 감지 → UI 렌더링, 로케일 전환 흐름 |
| **산출물** | 녹화 스크립트 원본 → POM 리팩토링 입력 소재 |

---

### 2-12. AI를 이용한 스크립트 고도화(Refactoring to POM) 계획

| 항목 | 내용 |
|------|------|
| **POM 분리** | `LocaleSettingsPage`(로케일 설정), `I18nApiClient`(다국어 API 호출), `UITextValidator`(화면 텍스트 검증) |
| **AI 역할** | 녹화 스크립트 → POM 클래스 자동 분리, 로케일 키 파라미터화 |

---

### 2-13. AI 데이터 모킹 및 네트워크 인터셉션(Data Mocking) 계획

| 항목 | 내용 |
|------|------|
| **i18n API 모킹** | WireMock — 베트남어 누락 키 응답 시뮬레이션 (fallback 처리 확인) |
| **CDN 모킹** | Noto Sans VI 폰트 CDN 차단 시 fallback 폰트 렌더링 확인 |
| **네트워크 인터셉션** | Playwright `route()` — `/api/common/i18n/messages` 응답 지연·오류 주입 |

---

### 2-14. 검증 및 디버깅(Local Execution & Debug) 계획

| 항목 | 내용 |
|------|------|
| **로컬 실행** | Docker Compose (이음톡 + 스테이징 DB + WireMock) |
| **디버깅 포인트** | `Accept-Language` 헤더 감지 로직, `SYS_MULTILANGUAGE` 조회, JSON 응답 조립 |
| **로그** | `DEBUG` 레벨 — 로케일 감지·DB 조회·응답 JSON 전체 로깅 |

---

### 2-15. CI/CD 파이프라인 통합 및 모니터링(CI Integration) 계획

| 항목 | 내용 |
|------|------|
| **CI 도구** | Jenkins / GitHub Actions (프로젝트 표준) |
| **트리거** | `SYS_MULTILANGUAGE` 데이터 변경 또는 이음톡 모듈 소스 변경 시 자동 실행 |
| **알림** | 베트남어 누락 키 발생 시 담당자 즉시 알림 |

---

### 2-16. 성공 지표 및 대시보드 계획

| 지표 | 목표 |
|------|------|
| 통합테스트 케이스 통과율 | 95% 이상 |
| 베트남어 UI 누락 키 | 0건 |
| 성조 문자 렌더링 깨짐 | 0건 |
| `/api/common/i18n/messages?lang=vi` 응답시간 | 2초 이내 |
| 로케일 전환 시 상호 오버라이드 | 0건 |

---

### 2-17. 이슈/리스크 예상 정보

| 번호 | 구분 | 내용 | 조치 방향 |
|------|------|------|---------|
| 1 | 🔴 고위험 | `SYS_MULTILANGUAGE` 베트남어 데이터 미입력 → UI 전체 키 미표시 | 테스트 진입 전 데이터 입력 완료 필수 |
| 2 | 🟡 중위험 | Noto Sans VI CDN 접근 불가(베트남 현지 네트워크) → 성조 문자 깨짐 | CDN URL 베트남 네트워크 접근 가능 여부 사전 확인 |
| 3 | 🟢 저위험 | 한국어 ↔ 베트남어 동시 접속 시 로케일 캐시 혼용 가능성 | SCN-005 집중 점검 |

---

### 2-18. 특이사항

- 소스 수정 불가 전제 — `SYS_MULTILANGUAGE` 데이터 입력, `ResourceBundle` `vi` 파일 배포, CDN 설정 기반 검증에 집중
- 베트남어 원어민 검수 권장 — QA 파트 배정 시 현지 사용자 참여 고려

---

**산출 근거**
- `500AI_F_010.REQ-EUM-003_통합테스트 요청서_202605131100_v1.0`
- `300AI_D_040.REQ-EUM-003_설계 상세 결과서_202604151530_v1.0`
- `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분析서_v1.5_20260414` (REQ-EUM-003)
