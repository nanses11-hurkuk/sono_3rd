# [통합테스트 계획서] REQ-WML-001

---

## (1) 기본 정보

| 항목 | 내용 |
|------|------|
| **요구사항 ID** | REQ-WML-001 |
| **서브시스템** | 웹메일 |
| **요구사항명** | exchange 기반 웹메일 구성 |
| **통합테스트 요청서 ID** | 500AI_F_010.REQ-WML-001_통합테스트 요청서_202605131100_v1.0 |
| **통합테스트 계획서 ID** | 500AI_F_020.REQ-WML-001_통합테스트 계획서_202605191100_v1.0 |
| **산출 근거** | - `400AI_E_130.REQ-WML-001_단위테스트 완료 PM 승인서_202605131000_v1.0` |
| | - `300AI_D_040.REQ-WML-001_설계 상세 결과서_202604151530_v1.0` |
| | - `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분석서_v1.5_20260414` (REQ-WML-001) |

---

## (2) 통합테스트 계획 범위

### 2-1. 통합테스트 목적 및 범위

| 항목 | 내용 |
|------|------|
| **목적** | Exchange 기반 웹메일 모듈(covimail ↔ smarts4j_coviexchange)의 계층 간 통합 동작 검증 및 계열사(V1/V2) 환경에서의 멀티 도메인 메일 처리 정합성 확인 |
| **범위** | UI → Controller → Service(MsMailSvcImpl) → RestMailCon → EWSCommon → Exchange Server 전 구간 |
| **제외** | 단위테스트에서 검증 완료된 개별 메서드 내부 로직, IMAP 기반 MailSvcImpl(Exchange 미대상) |

---

### 2-2. 난이도

> **중** — 계열사 간 domainId 분기(SvcImplFactory), OAuth2 토큰 갱신(MSAL4J), 대용량 첨부 업로드 세션 등 신규 구현 항목 포함

---

### 2-3. 개발자(담당자) 배정

| 역할 | 배정 요청 |
|------|---------|
| 통합테스트 주담당 개발자 | 단위테스트 담당 개발자 (WebMail 모듈) |
| QA | QA 파트 1인 배정 요청 |
| 인프라 협조 | Azure AD 앱 권한 확인, Exchange 서버 모던 인증 설정 (PM + 인프라팀) |

---

### 2-4. 계획일(시작/종료일)

| 항목 | 일정 |
|------|------|
| **시작일** | 2026-05-18 |
| **종료일** | 2026-05-22 |
| **비고** | 미결 항목(Azure AD 앱 등록, OAuth2 전환) 사전 해소 필요 |

---

### 2-5. 통합테스트 가능 여부

> **필수** — 단위테스트 PM 승인 완료. 단, 아래 사전 조건 충족 후 진입

| 사전 조건 | 상태 |
|---------|------|
| Azure AD 앱 등록 및 EWS.AccessAsApp 권한 확보 | ⚠️ 미결 — PM + 인프라팀 조치 필요 |
| setSession.ps1 OAuth2 전환 (Kerberos 제거) | ⚠️ 미결 — 개발자 + 인프라팀 조치 필요 |
| ExchangeConfigDTO OAuth2 필드 추가 및 Redis 키 정의 | ⚠️ 미결 — 개발자 조치 필요 |
| EWSCommon Bearer 토큰 주석 해제 (GAP-WML-001-02) | ⚠️ 미결 |

---

### 2-6. 통합테스트 환경 및 인프라 구축 계획

| 항목 | 내용 |
|------|------|
| **테스트 서버** | UTI VINA(V1) 및 UTI VINA Vinh Phuc(V2) 스테이징 환경 |
| **Exchange 연동** | Microsoft Exchange Server (EWS SOAP/HTTPS) + M365(Graph REST) |
| **Redis** | 토큰 캐시, 구독 TTL, 업로드 세션 키 — 스테이징 Redis 인스턴스 |
| **계열사 도메인 분리** | ResourceManager ConcurrentHashMap — V1/V2 domainId 각각 설정 |
| **다국어** | 한국어 / 영어 / 베트남어 3개 언어 환경 동시 점검 |
| **네트워크** | 베트남 현지 네트워크 환경 기준 (EWS SOAP HTTPS 443 포트 개방 확인) |

---

### 2-7. 통합테스트 데이터 전략

| 구분 | 내용 |
|------|------|
| **테스트 계정** | V1(UTI VINA) 테스트 메일 계정 3종, V2(Vinh Phuc) 테스트 메일 계정 3종 준비 |
| **메일 데이터** | 일반 메일, 대용량 첨부(25MB+), 멀티언어 제목/본문, 공유 메일함 포함 |
| **모킹 데이터** | Azure AD 토큰 만료 시나리오용 단기 TTL 토큰 mock, EWS 응답 지연 시뮬레이션 |
| **초기화** | 각 시나리오 실행 전 테스트 폴더 및 메일 상태 초기화 스크립트 수행 |

---

### 2-8. 통합테스트 상세 범위 계획

| 통합 레이어 | 검증 대상 | 주요 확인 항목 |
|------------|---------|-------------|
| **UI ↔ API Layer** | 브라우저 → MailConAjax → MsMailSvcImpl | 메일 목록/상세/발송 API 정상 응답, domainId 헤더 전달 여부 |
| **Service ↔ Service** | SvcImplFactory → MsMailSvcImpl / MailSvcImpl 분기 | domainId 기반 런타임 구현체 교체(GAP-WML-001-10) |
| **App ↔ Database** | ResourceManager ↔ Redis | 토큰 캐시 저장·갱신, 구독 TTL 관리(GAP-WML-001-07, 09) |
| **App ↔ Exchange** | RestMailCon → EWSCommon → Exchange EWS | 메일 CRUD 50+ 엔드포인트, Bearer 토큰 인증(GAP-WML-001-02) |
| **Async Tasks** | SubscriptionRenewalScheduler | 구독 만료 전 자동 갱신 트리거 및 Redis TTL 갱신 확인 |

---

### 2-9. AI 기반 통합테스트 시나리오 설계 계획

| 통합테스트 케이스 ID | 시나리오 ID | 핵심 검증 내용 | 테스트 페르소나 | 통과 기준 | 멀티 유저 시나리오 | 영향도 |
|-----------------|-----------|-------------|-------------|---------|---------------|------|
| TC-WML-INT-001 | SCN-001 | V1 사용자 메일 발송 → Exchange EWS 정상 처리 | V1 일반 사용자 | HTTP 200, 메일 Exchange 서버 도착 확인 | V1 3명 동시 발송 | 높음 |
| TC-WML-INT-002 | SCN-002 | V2 사용자 메일 수신 목록 조회 (페이지네이션) | V2 일반 사용자 | pageSize/page 기반 정상 목록 반환, 응답 3초 이내 | V2 5명 동시 조회 | 중 |
| TC-WML-INT-003 | SCN-003 | AccessToken 만료 → 자동 갱신 후 메일 발송 | V1 일반 사용자 | 토큰 갱신 성공, 메일 정상 발송, 사용자 오류 미노출 | - | 높음 |
| TC-WML-INT-004 | SCN-004 | 대용량 첨부(25MB) 업로드 세션 생성 및 청크 업로드 | V1 총무팀 | uploadSession 생성 → 청크 업로드 완료 → 발송 성공 | - | 중 |
| TC-WML-INT-005 | SCN-005 | 공유 메일함 폴더 조회 (isHidden 필터) | V1 관리자 | 숨김 폴더 미표시, 공유 폴더 정상 표시 | V1 2명 동시 접근 | 중 |
| TC-WML-INT-006 | SCN-006 | 다국어 환경(베트남어) 메일 제목/본문 처리 | V2 베트남 현지 사용자 | UTF-8 데이터 정합성 유지, 화면 깨짐 없음 | V1/V2 혼합 3명 | 중 |
| TC-WML-INT-007 | SCN-007 | 웹훅 구독 갱신 스케줄러 정상 동작 | 시스템(Scheduler) | 만료 30분 전 갱신, Redis TTL 갱신 확인 | - | 높음 |

---

### 2-10. AI 기반 통합테스트 실행 전략 계획

| 전략 | 내용 |
|------|------|
| **시나리오 합성** | SCN-001 + SCN-003 조합(토큰 만료 상태에서 발송) 등 복합 시나리오 AI 자동 생성 |
| **스크립트 자율 생성** | Playwright/Cypress 기반 E2E 스크립트 — API 명세(RestMailCon 50+ 엔드포인트) 기반 자동 생성 |
| **자가 치유(Self-Healing)** | UI 셀렉터 변경 시 AI 기반 자동 재매핑, EWS 응답 스키마 변동 감지 후 파싱 로직 자동 수정 |

---

### 2-11. 기본 코드 녹화(Recording) 계획

| 항목 | 내용 |
|------|------|
| **도구** | Playwright Codegen |
| **대상** | 메일 목록 조회, 메일 발송, 폴더 관리, 설정(자동회신/규칙) 주요 흐름 녹화 |
| **산출물** | 녹화 스크립트 원본 → AI 리팩토링 입력 소재 |

---

### 2-12. AI를 이용한 스크립트 고도화(Refactoring to POM) 계획

| 항목 | 내용 |
|------|------|
| **패턴** | Page Object Model (POM) 구조로 리팩토링 |
| **AI 역할** | 녹화 스크립트 분석 → Page Class 자동 분리, 공통 헬퍼 추출, 파라미터화 |
| **관리** | GitHub 저장소 내 `tests/webmail/pages/` 경로 구조화 |

---

### 2-13. AI 데이터 모킹 및 네트워크 인터셉션(Data Mocking) 계획

| 항목 | 내용 |
|------|------|
| **EWS 응답 모킹** | EWS SOAP 응답 XML 모킹 — 타임아웃, 인증 오류, 대용량 응답 시뮬레이션 |
| **토큰 만료 모킹** | MSAL4J 토큰 단기 TTL 설정 → GAP-WML-001-02 갱신 로직 강제 실행 |
| **네트워크 인터셉션** | Playwright `route()` API로 `/v1/mail/**` 요청 가로채기 — 응답 지연/오류 주입 |
| **Redis 모킹** | 토큰 캐시 미존재 시나리오 강제 구성 |

---

### 2-14. 검증 및 디버깅(Local Execution & Debug) 계획

| 항목 | 내용 |
|------|------|
| **로컬 실행** | 개발자 로컬 환경 — Docker Compose (covimail + smarts4j_coviexchange + Redis + Mock Exchange) |
| **디버깅 포인트** | EWSCommon Bearer 토큰 주석 해제 후 인증 흐름, SvcImplFactory domainId 분기 로직 |
| **로그 수준** | `DEBUG` 레벨 강제 — EWS 요청/응답 SOAP XML 전체 로깅 |
| **리포트** | Playwright HTML Report 자동 생성, 실패 시 스크린샷 + 네트워크 로그 첨부 |

---

### 2-15. CI/CD 파이프라인 통합 및 모니터링(CI Integration) 계획

| 항목 | 내용 |
|------|------|
| **CI 도구** | Jenkins / GitHub Actions (프로젝트 표준 따름) |
| **트리거** | 웹메일 모듈 소스 변경 시 자동 통합테스트 실행 |
| **병렬 실행** | V1/V2 환경 병렬 실행 — 총 실행 시간 30분 이내 목표 |
| **알림** | 실패 시 담당 개발자 + QA 이메일/메신저 알림 |
| **모니터링** | EWS 응답시간, Redis 캐시 히트율, 토큰 갱신 주기 — 대시보드 연동 |

---

### 2-16. 성공 지표 및 대시보드 계획

| 지표 | 목표 |
|------|------|
| 통합테스트 케이스 통과율 | 95% 이상 |
| 메일 발송/수신 API 응답시간 | 3초 이내 (P95) |
| AccessToken 자동 갱신 성공률 | 100% |
| 대용량 첨부 업로드 성공률 | 100% (25MB 기준) |
| V1/V2 계열사 간 데이터 정합성 | 0건 오류 |
| 다국어(베트남어) 데이터 깨짐 | 0건 |

---

### 2-17. 이슈/리스크 예상 정보

| 번호 | 구분 | 내용 | 조치 방향 |
|------|------|------|---------|
| 1 | 🔴 고위험 | Azure AD 앱 등록 미완 → GAP-WML-001-02 검증 불가 | PM + 인프라팀 사전 조치 필수 |
| 2 | 🟡 중위험 | setSession.ps1 Kerberos → OAuth2 전환 미완 시 PowerShell 연동 실패 | 개발자 + 인프라팀 협의 |
| 3 | 🟡 중위험 | V1/V2 계열사 간 domainId 혼용 시 메일 발송 오발송 가능성 | SvcImplFactory 분기 로직 집중 검증 |
| 4 | 🟡 중위험 | 베트남 현지 네트워크 환경에서 EWS SOAP HTTPS 응답 지연 | 네트워크 인터셉션 모킹으로 보완 |
| 5 | 🟢 저위험 | SubscriptionRenewalScheduler 실행 타이밍 — 스테이징 환경 TTL 조정 필요 | TTL 단축 설정으로 검증 가속 |

---

### 2-18. 특이사항

- GAP-WML-001-06(메일 목록 페이지네이션)은 단위테스트에서 기 검증 완료(`MailSvcImpl.java:446~448`) — 통합테스트에서는 실제 Exchange 서버 연동 시 동작 확인만 수행
- EWS 타임아웃(30초, `EWSCommon.java:158`)은 기 적용 상태 — 베트남 현지 네트워크 기준 타임아웃 적정성 재확인 권장
- 다국어 데이터(한국어/영어/베트남어) 정합성은 계열사 간 연동 시나리오에서 함께 점검

---

**산출 근거**
- `500AI_F_010.REQ-WML-001_통합테스트 요청서_202605131100_v1.0`
- `300AI_D_040.REQ-WML-001_설계 상세 결과서_202604151530_v1.0`
- `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분석서_v1.5_20260414` (REQ-WML-001)
- `400AI_E_130.REQ-WML-001_단위테스트 완료 PM 승인서_202605131000_v1.0`
