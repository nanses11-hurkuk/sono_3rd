# [통합테스트 계획서] REQ-ODD-001

---

## (1) 기본 정보

| 항목 | 내용 |
|------|------|
| **요구사항 ID** | REQ-ODD-001 |
| **서브시스템** | Outlook Add-in |
| **요구사항명** | Outlook Add-in 구성 |
| **요구사항 상세** | 대용량 첨부, 조직도 포함 |
| **통합테스트 요청서 ID** | 500AI_F_010.REQ-ODD-001_통합테스트 요청서_202605131100_v1.0 |
| **통합테스트 계획서 ID** | 500AI_F_020.REQ-ODD-001_통합테스트 계획서_202605191100_v1.0 |
| **산출 근거** | - `400AI_E_130.REQ-ODD-001_단위테스트 완료 PM 승인서_202605131000_v1.0` |
| | - `300AI_D_040.REQ-ODD-001_설계 상세 결과서_202604151530_v1.0` |
| | - `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분석서_v1.5_20260414` (REQ-ODD-001) |

---

## (2) 통합테스트 계획 범위

### 2-1. 통합테스트 목적 및 범위

| 항목 | 내용 |
|------|------|
| **목적** | Outlook Add-in(OutlookAddinCon → OutlookAddinSvcImpl → CoviMSAPI → smarts4j_coviexchange) 전 계층 통합 동작 검증. 대용량 첨부·조직도 비동기 조회·연락처(배포그룹 포함)·배포 그룹 관리 기능의 계열사(V1/V2) 환경 정합성 확인 |
| **범위** | Outlook 클라이언트(CAUTK) → `/outlook/service.do` → OutlookAddinSvcImpl → CoviMSAPI(org/contact) → smarts4j_coviexchange(RestGroupCon/RestContactCon) → AD/Exchange 전 구간 |
| **제외** | 단위테스트에서 검증 완료된 ContactSvcImpl·OrgSvc 내부 메서드 로직 |

---

### 2-2. 난이도

> **중** — G-01(세션 재연결), G-02(OAuth2 전환), G-05(Redis Distributed Lock), G-06(비동기 조직도) 신규 구현 항목 포함. Azure AD 앱 권한 미결로 G-02 인프라 의존성 존재.

---

### 2-3. 개발자(담당자) 배정

| 역할 | 배정 요청 |
|------|---------|
| 통합테스트 주담당 개발자 | 단위테스트 담당 개발자 (Outlook Add-in 모듈) |
| QA | QA 파트 1인 배정 요청 |
| 인프라 협조 | Azure AD 앱 권한(`Contacts.ReadWrite`, `Group.ReadWrite.All`) 확인, Exchange 서버 모던 인증 설정 (PM + 인프라팀) |

---

### 2-4. 계획일(시작/종료일)

| 항목 | 일정 |
|------|------|
| **시작일** | 2026-05-18 |
| **종료일** | 2026-05-22 |
| **비고** | G-02(OAuth2) 관련 Azure AD 앱 등록 및 `setSession.ps1` 모던 인증 전환 사전 조치 필요 |

---

### 2-5. 통합테스트 가능 여부

> **필수** — 단위테스트 PM 승인 완료. 단, 아래 사전 조건 충족 후 진입

| 사전 조건 | 상태 |
|---------|------|
| Azure AD 앱 권한(`Contacts.ReadWrite`, `Group.ReadWrite.All`) 확보 | ⚠️ 미결 — PM + 인프라팀 조치 필요 |
| `setSession.ps1` OAuth2 전환 (Kerberos 제거) | ⚠️ 미결 — 개발자 + 인프라팀 조치 필요 |
| CoviMSAPI `baseUrl` 개발/운영 환경별 분리 설정 확인 | ⚠️ 미결 — 개발자 조치 필요 |
| Redis Distributed Lock 적용 확인 (G-05) | ⚠️ 미결 |

---

### 2-6. 통합테스트 환경 및 인프라 구축 계획

| 항목 | 내용 |
|------|------|
| **테스트 서버** | UTI VINA(V1) 및 UTI VINA Vinh Phuc(V2) 스테이징 환경 |
| **Outlook 클라이언트** | COM Add-in / Web Add-in 테스트용 Outlook 클라이언트 (Windows 환경) |
| **Exchange 연동** | Active Directory (LDAP 389/636), Exchange Server (EWS SOAP/HTTPS), Exchange Remote PowerShell |
| **Redis** | Distributed Lock, 비동기 결과 저장, 토큰 캐시 — 스테이징 Redis 인스턴스 |
| **CoviMSAPI baseUrl** | 개발 환경 coviexchange 서버 URL 분리 설정 확인 |
| **다국어** | 한국어 / 영어 / 베트남어 3개 언어 조직도·연락처 데이터 동시 점검 |

---

### 2-7. 통합테스트 데이터 전략

| 구분 | 내용 |
|------|------|
| **테스트 계정** | V1/V2 내부 사용자 계정 각 3종, External(외부 vendor) 계정 1종 |
| **조직도 데이터** | V1/V2 조직 구조(수동 구성), 다국어 부서명·사용자명 포함 |
| **연락처 데이터** | 개인 연락처, 부서 연락처, 회사 연락처, 배포 그룹 — 각 유형별 테스트 데이터 준비 |
| **배포 그룹 데이터** | LDAP + Exchange 배포 그룹 테스트용 그룹 3종 (CRUD 검증용) |
| **대용량 첨부** | 25MB 이상 첨부 파일 테스트 데이터 |
| **초기화** | 각 시나리오 실행 전 배포 그룹·연락처·세션 상태 초기화 스크립트 수행 |

---

### 2-8. 통합테스트 상세 범위 계획

| 통합 레이어 | 검증 대상 | 주요 확인 항목 |
|------------|---------|-------------|
| **UI ↔ API Layer** | Outlook 클라이언트 CAUTK → `/outlook/service.do` OutlookAddinCon | type별(GETORGCHART/GETCONTACTS/MANAGEDISTGROUP/GETBIGATTACH) 파라미터 파싱 및 라우팅 |
| **Service ↔ Service** | OutlookAddinSvcImpl → CoviMSAPI(org/contact) → smarts4j_coviexchange | G-03(배포그룹 CRUD), G-04/G-07(연락처 조회) 연동 레이어 동작 |
| **App ↔ Database** | LdapManager ↔ Active Directory, Redis ↔ 세션/토큰/비동기결과 | G-01 LDAP 세션 재연결(최대 3회 Exponential Backoff), G-05 Distributed Lock |
| **App ↔ Exchange** | EWSCommon → Exchange EWS, ExchGroupSvcImpl → Exchange PowerShell | 배포 그룹 메일 활성화(enableGroup), EWS 연락처 CRUD |
| **Async Tasks** | `@Async("outlookAsyncExecutor")` getOrgChartAsync → Redis 결과 저장 → 폴링 API | G-06 비동기 조직도 조회 — taskId 발급 → PENDING/DONE 상태 전이 → 결과 반환 |

---

### 2-9. AI 기반 통합테스트 시나리오 설계 계획

| 통합테스트 케이스 ID | 시나리오 ID | 핵심 검증 내용 | 테스트 페르소나 | 통과 기준 | 멀티 유저 시나리오 | 영향도 |
|-----------------|-----------|-------------|-------------|---------|---------------|------|
| TC-ODD-INT-001 | SCN-001 | GETORGCHART 비동기 조직도 조회 — taskId 발급 → DONE 상태 전이 → V1/V2 전체 조직 표시 | V1 일반 사용자 | taskId 발급 성공, Redis 결과 저장, 조직도 정상 반환, 응답 5초 이내 | V1 3명 동시 조회 | 높음 |
| TC-ODD-INT-002 | SCN-002 | GETCONTACTS 연락처 4종(개인/부서/회사/배포그룹) XML 응답 | V1 일반 사용자 | CPAddressBook XML 4개 GroupName 정상 반환, 배포그룹 포함 | V1/V2 3명 동시 | 중 |
| TC-ODD-INT-003 | SCN-003 | MANAGEDISTGROUP — 배포 그룹 CREATE→MODIFY→DELETE 순차 CRUD | V1 관리자 | LDAP + Exchange enableGroup 이중 처리 성공, 각 opType 정상 응답 | — | 높음 |
| TC-ODD-INT-004 | SCN-004 | MANAGEDISTGROUP — ADD_MEMBER / REMOVE_MEMBER (GWGroupMemberDTO 처리) | V1 관리자 | 그룹 멤버 추가·제거 LDAP 반영 확인 | V1 2명 동시 | 중 |
| TC-ODD-INT-005 | SCN-005 | G-01 Exchange/LDAP 세션 재연결 — 세션 만료 후 자동 재연결(최대 3회) | 시스템(자동) | 3회 이내 재연결 성공, 사용자 오류 미노출 | — | 높음 |
| TC-ODD-INT-006 | SCN-006 | G-05 Redis Distributed Lock — 동시 세션 생성 중복 방지 | V1 3명 동시 | Lock 경합 시 순차 처리, 중복 세션 0건 | 3명 동시 요청 | 중 |
| TC-ODD-INT-007 | SCN-007 | GETBIGATTACH 대용량 첨부(25MB+) 다운로드 | V1 일반 사용자 | 첨부 파일 정상 다운로드, 타임아웃 없음 | — | 중 |
| TC-ODD-INT-008 | SCN-008 | 다국어(베트남어) 조직도·연락처 데이터 UTF-8 정합성 | V2 베트남 현지 사용자 | 베트남어 데이터 화면 깨짐 없음, XML 정합성 유지 | V1/V2 혼합 3명 | 중 |

---

### 2-10. AI 기반 통합테스트 실행 전략 계획

| 전략 | 내용 |
|------|------|
| **시나리오 합성** | SCN-001 + SCN-002 조합(조직도 조회 중 연락처 동시 요청), SCN-003 + SCN-004 조합(배포 그룹 생성 후 멤버 추가) 복합 시나리오 AI 자동 생성 |
| **스크립트 자율 생성** | Playwright 기반 E2E 스크립트 — `/outlook/service.do` CAUTK 파라미터 조합 및 XML 응답 파싱 자동 생성 |
| **자가 치유(Self-Healing)** | CAUTK 파라미터 구조 변경 감지 시 자동 재매핑, XML 응답 스키마 변동 시 파싱 로직 자동 수정 |

---

### 2-11. 기본 코드 녹화(Recording) 계획

| 항목 | 내용 |
|------|------|
| **도구** | Playwright Codegen + Fiddler (CAUTK HTTP 요청 캡처) |
| **대상** | GETORGCHART(비동기), GETCONTACTS, MANAGEDISTGROUP(CREATE/ADD_MEMBER), GETBIGATTACH 핵심 흐름 |
| **산출물** | 녹화 스크립트 원본 → AI 리팩토링 입력 소재 |

---

### 2-12. AI를 이용한 스크립트 고도화(Refactoring to POM) 계획

| 항목 | 내용 |
|------|------|
| **패턴** | Page Object Model (POM) 구조 리팩토링 |
| **AI 역할** | CAUTK 요청 빌더, XML 응답 파서, 배포 그룹 관리 헬퍼 분리 및 파라미터화 |
| **관리** | `tests/outlook-addin/pages/` 경로 구조화 |

---

### 2-13. AI 데이터 모킹 및 네트워크 인터셉션(Data Mocking) 계획

| 항목 | 내용 |
|------|------|
| **LDAP/EWS 응답 모킹** | WireMock — LDAP 세션 만료, EWS 타임아웃, PowerShell 오류 응답 시뮬레이션 (G-01 재연결 검증) |
| **OAuth2 토큰 모킹** | Azure AD 앱 미등록 대비 — 단기 TTL mock 토큰으로 G-02 인증 흐름 부분 검증 |
| **네트워크 인터셉션** | Playwright `route()` API로 `/outlook/service.do` 요청 가로채기 — 응답 지연·오류 주입 |
| **Redis 모킹** | Lock 경합 시나리오 강제 구성 (G-05 Distributed Lock 검증) |
| **비동기 결과 모킹** | PENDING 장기 유지 시나리오 — 폴링 타임아웃 처리 확인 (G-06) |

---

### 2-14. 검증 및 디버깅(Local Execution & Debug) 계획

| 항목 | 내용 |
|------|------|
| **로컬 실행** | Docker Compose (covimail + smart4j_msapi + smarts4j_coviexchange + Redis + WireMock AD/Exchange) |
| **디버깅 포인트** | ExchangeSessionManager 재시도 로직, Redis Lock setnx 흐름, getOrgChartAsync taskId 발급 및 폴링 |
| **로그 수준** | `DEBUG` 레벨 — LDAP 요청·EWS SOAP XML·CAUTK 파라미터 파싱 전체 로깅 |
| **리포트** | Playwright HTML Report, 실패 시 스크린샷 + CAUTK 요청/응답 로그 첨부 |

---

### 2-15. CI/CD 파이프라인 통합 및 모니터링(CI Integration) 계획

| 항목 | 내용 |
|------|------|
| **CI 도구** | Jenkins / GitHub Actions (프로젝트 표준 따름) |
| **트리거** | Outlook Add-in 모듈 소스 변경 시 자동 통합테스트 실행 |
| **병렬 실행** | V1/V2 환경 병렬 실행 — 총 실행 시간 30분 이내 목표 |
| **알림** | 실패 시 담당 개발자 + QA 알림 |
| **모니터링** | 비동기 조직도 응답시간, Redis Lock 대기시간, LDAP 재연결 횟수 대시보드 연동 |

---

### 2-16. 성공 지표 및 대시보드 계획

| 지표 | 목표 |
|------|------|
| 통합테스트 케이스 통과율 | 95% 이상 |
| GETORGCHART 비동기 응답시간 | 5초 이내 (P95) |
| GETCONTACTS XML 응답 정합성 | 4종 GroupName 100% 포함 |
| LDAP/EWS 세션 재연결 성공률 | 100% (3회 이내) |
| 배포 그룹 CRUD 성공률 | 100% |
| 대용량 첨부(25MB+) 다운로드 성공률 | 100% |
| 다국어(베트남어) 데이터 깨짐 | 0건 |
| Redis Distributed Lock 중복 세션 | 0건 |

---

### 2-17. 이슈/리스크 예상 정보

| 번호 | 구분 | 내용 | 조치 방향 |
|------|------|------|---------|
| 1 | 🔴 고위험 | Azure AD 앱 권한 미확보 → G-02 OAuth2AuthStrategy 실 연동 검증 불가 | PM + 인프라팀 `Contacts.ReadWrite`, `Group.ReadWrite.All` 권한 사전 확보 필수 |
| 2 | 🔴 고위험 | `setSession.ps1` Kerberos 고정 → OAuth2 전환 미완 시 Exchange PowerShell 연동 실패 | 개발자 + 인프라팀 모던 인증 설정 협의 |
| 3 | 🟡 중위험 | LdapManager 매 요청 LdapTemplate 재생성 → 부하 시 세션 풀 고갈 가능 | G-01 ExchangeSessionManager 재시도 래핑 집중 검증 |
| 4 | 🟡 중위험 | CoviMSAPI `baseUrl` 환경 분리 미완 시 운영 Exchange 호출 가능성 | 개발 환경 URL 분리 설정 사전 확인 |
| 5 | 🟡 중위험 | 비동기 조직도(G-06) 폴링 타임아웃 — 베트남 현지 네트워크 환경 지연 가능 | 타임아웃 임계값 협의 및 모킹으로 사전 검증 |
| 6 | 🟢 저위험 | 다국어 조직도·연락처 UTF-8 데이터 깨짐 — Exchange/LDAP 응답 인코딩 이슈 가능 | TC-ODD-INT-008 집중 점검 |

---

### 2-18. 특이사항

- `OutlookAddinSvcImpl.java:677, 717` `getContacts()` TODO 2건은 설계 완료(G-04, G-07) — 통합테스트 전 개발 완성 여부 확인 필수
- `GWGroupMemberDTO.m365GroupManageTool` 필드로 Graph/PowerShell 처리 방식 런타임 선택 가능 — 베트남 환경에 맞게 설정 값 사전 협의 필요
- 비동기 조직도 폴링 API(`GET /outlook/asyncResult.do`) 는 G-06 신규 구현 — 단위테스트 완료 여부 재확인 후 통합테스트 진입

---

**산출 근거**
- `500AI_F_010.REQ-ODD-001_통합테스트 요청서_202605131100_v1.0`
- `300AI_D_040.REQ-ODD-001_설계 상세 결과서_202604151530_v1.0`
- `231.유티아이베트남 그룹웨어 시스템 구축_요구사항정의 및 GAP분석서_v1.5_20260414` (REQ-ODD-001)
- `400AI_E_130.REQ-ODD-001_단위테스트 완료 PM 승인서_202605131000_v1.0`
